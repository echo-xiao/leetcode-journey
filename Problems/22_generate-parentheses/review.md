# 22. 括号生成 · 复盘

难点在于条件，什么时候（，什么时候 ），是一个带着限制条件的路径搜索，不能随时添加任何字符，每一步的选择都受限于之前的决策。这个是**路径填充**的问题。**这个不是**选或者不选通常出现在子集问题里面，**一个选或者不选的问题，是A和B里面，有条件的二选一**。这个条件就是 right < left ≤ n，并且这个里面存在大量的非法路径。难点在于通过 left < n，和 right < left 这两个动态变量，实时封死所有的通往非法字符串的路径。17、letter combinations of phone number 的递归树是整齐划一的，但是这个里面的递归树是非对称的斜树，一开始只能加（，不能加 ），只有随着left增加，加 （的空间才慢慢释放出来，递归逻辑的难点在于**动态变化**的选择空间。这个里面left和right表示的是左括号和右括号的计数器，**left+right就是层数**，每一层只能够根据条件选择（ 或者 ），选择了就+1。#组合

# 22. 括号生成 · 要素

1. 路径：path 就是当前拼到一半的括号串，比如已经放了 "(()"，同时用 left、right 记住里面各有几个左右括号。

2. 选择列表：每一步最多两个选择：左括号还没用满（left<n）就能加 "("，右括号数少于左括号数（right<left）就能加 ")"。

3. 结束条件：path 长度到 2*n 时说明 n 对括号都放完且一路合法，把它转成字符串塞进 res 然后 return。

4. 撤销：递归回来后把刚加的那个字符从 path 末尾删掉，left/right 因为是按参数传值的所以自动还原，不用手动改。

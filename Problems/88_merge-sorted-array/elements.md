# 88. 合并两个有序数组 · 要素

1. 指针类型：用相向而行的双指针，但方向是从后往前：p1指向nums1有效元素末尾m-1，p2指向nums2末尾n-1，i指向nums1总长度末尾m+n-1

2. slow 含义：本题不涉及slow指针，因为不是同向快慢指针场景，而是两个数组各自从末尾往前移动的指针

3. 停止条件：当p1<0或p2<0时停止主循环（其中一个数组比完了），如果p2还有剩余（p2>=0）就继续把nums2剩下的元素抄到nums1前面，直到p2<0

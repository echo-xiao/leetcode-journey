# 239. 滑动窗口最大值 · 要素

1. 单调方向：队列从队头到队尾保持单调递减（存的是nums值对应的下标，值递减）

2. 存下标还是值：存下标，因为要判断下标是否滑出窗口范围，同时能通过下标取回nums值

3. 弹栈条件：当队尾下标对应的值nums[q队尾] <= nums[i]时弹出队尾，因为它比新元素小且更早过期，不可能再成为最大值

4. 遍历方向：正着从i=0到n-1遍历数组

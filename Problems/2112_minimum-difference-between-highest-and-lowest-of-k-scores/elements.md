# 2112. 学生分数的最小差值 · 要素

1. 贪心策略：先把 nums 从小到大排序，然后只在排序后相邻的 k 个数里选，取 nums[i]-nums[i-k+1] 的最小值。

2. 局部最优判据：每次窗口右端固定在 i 时，直接用 nums[i]-nums[i-k+1] 当这一步的差值，比当前 min_diff 小就更新，不用管后面的窗口。

3. 正确性理由：选出的 k 个人的差值只由最大值和最小值决定，若这 k 个数在排序后不连续，把中间跳过的数换进来只会让两端更靠近或不变，所以最优解一定是排序后连续的 k 个，枚举全部 n-k+1 个连续窗口就必然覆盖到最优答案。

# 2866. 最长奇偶子数组 · 要素

1. 定长还是变长：变长窗口，因为奇偶交替子数组的长度不固定，要找最长的一段

2. 进窗口更新：当nums[r]是偶数且不超过threshold时才开始/继续扩展，只要nums[r]与nums[r-1]奇偶性不同且nums[r]<=threshold就一直右移r，扩大窗口

3. 出窗口时机：一旦不满足奇偶交替或者超过threshold，就把l直接跳到r（窗口整体右移到新起点），不是逐个缩小左边界

4. 记结果时机：每次成功扩展一次窗口（r右移时）就用r-l更新一次max_cnt，取最大值作为答案

# 567. 字符串的排列 · 要素

1. 定长还是变长：定长窗口，窗口大小固定为len(s1)，因为只需要判断s2中是否存在长度等于s1的子串是它的排列

2. 进窗口更新：每次right右移一格就把s2[right]计入win字典对应字符的计数加1，然后判断窗口是否超过k需要收缩

3. 出窗口时机：当窗口长度大于k（即i>=k）时就要把最左边的字符s2[i-k]从win中减1，如果减到0就把这个键删掉，保证窗口始终是k长度

4. 记结果时机：每次窗口凑满k个字符后（扩窗、可能已收缩完）就比较一次win和seen是否相等，只要相等就立刻返回True，说明找到了排列

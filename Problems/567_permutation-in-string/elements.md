# 567. 字符串的排列 · 要素

1. 定长还是变长：定长窗口，窗口大小固定为s1的长度len(s1)

2. 进窗口更新：right每次向右移动一位就把s2[right]计入s2Count对应字符的计数加一

3. 出窗口时机：当窗口长度right-left大于s1长度时就要把s2[left]的计数减一（减到0就删掉这个键），然后left加一，保证窗口始终不超过s1长度

4. 记结果时机：每次窗口长度恰好等于s1长度时就比较s1Count和s2Count是否相等，相等就返回True，遍历完还没匹配就返回False

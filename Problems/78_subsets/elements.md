# 78. 子集 · 要素

1. 路径：path数组，表示当前已经选进子集里的那些nums元素

2. 选择列表：从start到nums.length-1这些下标对应的元素，表示当前还能往子集里加的元素

3. 结束条件：没有显式的结束条件，因为每个path状态本身都是一个合法子集，所以每次进入dfs就直接把path加入res，start超出数组长度时循环自然不执行直接返回

4. 撤销：撤销的是path的最后一个元素，即把刚加进path的nums[i]弹出，恢复到递归前的路径状态

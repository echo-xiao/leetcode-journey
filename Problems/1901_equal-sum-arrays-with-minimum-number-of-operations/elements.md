# 1901. 通过最少操作次数使数组的和相等 · 要素

1. 贪心策略：把两数组元素能带来的最大差值改变量（大数组元素变1能减少x-1，小数组元素变6能增加6-y）汇总排序，每次挑改变量最大的元素去操作

2. 局部最优判据：局部最优就是每一步都选当前gains列表里数值最大的那个改变量，不用管后面还剩多少差值

3. 正确性理由：因为每次操作独立且互不影响，要用最少次数补齐固定的diff，肯定优先用单次贡献最大的操作，这样用的操作次数一定最少（类似用最大面值凑钱，次数最少）

# 2211. 半径为 k 的子数组平均值 · 要素

1. 定长还是变长：定长窗口，窗口大小固定为2k+1个元素

2. 进窗口更新：i每往右移一步就把nums[i+k]加入windowSum，也就是窗口右端点跟着i+k同步右移

3. 出窗口时机：i每往右移一步（i>k时）就把nums[i-k-1]从windowSum中减掉，即左端点跟着i-k-1同步右移，保持窗口长度恒为2k+1

4. 记结果时机：每次窗口和windowSum更新完（对每个满足k<=i<=n-k-1的i）就立刻算res[i]=windowSum//(2k+1)，不需要等窗口结束

# 100. 相同的树 · 要素

1. 函数定义：isSameTree(p, q) 同时接两棵树的当前节点，返回布尔值：以 p、q 为根的这两棵子树是否结构和值完全一样。

2. base case：p 和 q 都为空返回 True；只有一个为空返回 False（结构不同）。

3. 单层主体：当前节点都非空时先比 p.val 和 q.val，不等直接 False，相等就要求左对左、右对右两次递归都为 True。

4. 代码位置：前序位置：先比当前两个节点的值，再往下递归左右孩子，最后把两个子结果用与连接返回。

# 2843. 从 Rope 树中提取第 K 个字符 · 要素

1. 函数定义：dfs(node) 表示遍历以node为根的子树，把叶子节点的字符按中序（从左到右）拼接进全局res字符串，没有返回值（利用外部变量res）

2. base case：node为空直接返回，不做任何拼接（空节点没有字符可加）

3. 单层主体：如果当前node是叶子节点（没有左右孩子），就把node.val拼接到res里；如果不是叶子就不处理，交给左右子树递归去拼

4. 代码位置：中序位置：先递归左子树，再判断当前节点是否叶子并拼接，最后递归右子树，这样保证拼出来的字符顺序和原字符串一致

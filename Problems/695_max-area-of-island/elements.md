# 695. 岛屿的最大面积 · 要素

1. 节点与邻居：每个值为1的格子是一个节点，上下左右四个方向相邻且也是1的格子是邻居。

2. 起点与终点：起点是网格中每个未访问过的陆地格子（作为一次新岛屿BFS的起点），没有明确终点，BFS会把整个连通岛屿都探索完（队列空了就结束）。

3. visited 时机：格子一入队（刚被发现是陆地邻居时）就立刻标记visited，避免同一格子被多次加入队列重复计算面积。

4. 是否分层：本题不涉及分层，因为只需要统计连通块大小（面积），不需要区分BFS的层数或步数。

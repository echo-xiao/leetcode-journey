# 2232. 向字符串添加空格 · 要素

1. 指针类型：用同向（这里是从后往前）的双指针：i 扫 s 的字符，j 扫 spaces 数组，两者一起往前走，不是相向而行的左右指针。

2. slow 含义：起写入作用的是 k，它指向结果数组 res 里下一个待填的位置（从 n-1 往前填）；j 则表示当前还没处理的最靠后的那个空格下标。

3. 停止条件：i 走到 -1（s 的字符全部搬完）就结束循环，j 只是配合判断 i==spaces[j]，j 先耗尽也不影响，不存在相遇终止的说法。

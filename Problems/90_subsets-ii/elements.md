# 90. 子集 II · 要素

1. 路径：path数组，记录当前已经选入子集的元素组成的路径

2. 选择列表：从当前索引idx到数组末尾的元素，且同层中和前一个相同的元素要跳过（i>idx且nums[i]==nums[i-1]时跳过）

3. 结束条件：没有显式的结束条件，每次进入dfs都先把当前path加入res，遍历完idx到末尾就自然返回（没有硬性终止判断，靠for循环遍历完退出）

4. 撤销：撤销的是path末尾刚加入的那个元素，把它pop掉，恢复到递归前的路径状态

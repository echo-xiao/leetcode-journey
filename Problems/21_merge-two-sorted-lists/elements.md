# 21. 合并两个有序链表 · 要素

1. 要不要 dummy：要，因为要拼出一条新链表，用 dummy 当头就不用单独判断第一个节点是取 list1 还是 list2，最后返回 dummy.next。

2. 指针关系：这题不是快慢指针，是 list1、list2 两条链各一个指针加一个 curr 尾指针：谁的值小就把谁接到 curr 后面并让那条链的指针后移，curr 始终指向已合并部分的最后一个节点。

3. 停止条件：当 list1 或 list2 任一为 null 时结束循环，然后把 curr.next 直接指向另一条还没走完的链表剩余部分。

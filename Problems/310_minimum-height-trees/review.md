# 310. 最小高度树 · 复盘

这个题目是**拓扑排序的变种**，标准的Kahn算法，找到入度为0的点（没有前驱的点），从起点往终点剥离；310 MHT算法，入度为1的点（边缘叶子节点），从四周往中心剥离，Kahn是一个bfs算法，同时这个题目要理解**二叉树层序遍历的三种写法**。同时这个题目也是**一个最长路径问题（不是最短路径），因为中点肯定是最长路径的中点**，所以用两次dfs来找最长的路径在哪里就好了，dfs可以用迭代和递归两种方式来做。拓扑排序都没有搞清楚，晚点再看这道题目吧。**#topologicalSort**

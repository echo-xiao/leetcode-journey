# 643. 子数组最大平均数 I · 要素

1. 定长还是变长：定长窗口，长度固定为k，窗口大小不变，只是整体向右滑动

2. 进窗口更新：right每次向右移动一格就把nums[right]加进ksum里，扩大或滑动窗口时都要把新元素累加进和

3. 出窗口时机：每次right前进后，同时把left右移一位，并从ksum里减去nums[left]（滑出的旧元素），因为窗口长度固定必须同步移出一个元素

4. 记结果时机：每次更新完ksum后立刻用ksum和msum比较，取较大值更新msum，最后返回msum/k

# 107. 二叉树的层序遍历 II · 要素

1. 函数定义：本题用的是迭代 BFS，函数就是 levelOrderBottom(root)，输入根节点，返回一个二维列表，每个子列表是一层的节点值，且从最底层往上排。

2. base case：root 为空时直接返回空列表 []；队列空了就结束循环。

3. 单层主体：取出当前队列长度作为这一层的节点数，把这批节点依次弹出、值收进 vals，同时把它们的非空左右孩子按序入队，循环结束把 vals 追加到 ans。

4. 代码位置：本题不涉及前中后序，因为是队列层序遍历；唯一类似的处理是全部层收集完后把 ans 整体反转再返回。

# 219. 存在重复元素 II · 要素

1. 定长还是变长：定长窗口，窗口大小固定为k（其实是最多k个元素，下标间距不超过k）

2. 进窗口更新：遍历到nums[i]时先判断它是否已在集合seen里，不在就把它加入seen

3. 出窗口时机：当seen的大小超过k（即i>=k）时，把窗口最左边的元素nums[i-k]从seen中移除，保持窗口大小不超过k

4. 记结果时机：一旦发现当前元素nums[i]已经在seen中，立刻返回True，遍历结束都没发现就返回False

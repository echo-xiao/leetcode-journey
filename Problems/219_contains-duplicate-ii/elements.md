# 219. 存在重复元素 II · 要素

1. 定长还是变长：定长，窗口就是当前下标 j 往前数 k 个元素（集合大小最多 k）。

2. 进窗口更新：每次循环 right（即 j）都往右挪一格，把 nums[j] 加进 seen 集合，除此之外不需要维护别的量。

3. 出窗口时机：当 seen 里元素个数超过 k 时，把离开窗口的 nums[j-k] 从集合里删掉，集合大小回到 k。

4. 记结果时机：在把 nums[j] 加入集合之前先查，若 nums[j] 已在 seen 里就立刻返回 True；整个数组走完都没撞上就返回 False。

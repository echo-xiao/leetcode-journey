# 506. 相对名次 · 要素

1. 贪心策略：每次从剩余分数里挑出当前最大的分数，按从大到小的顺序依次分配名次1、2、3……

2. 局部最优判据：当前堆顶（负值最小即原值最大）的分数就是还没分配名次里排名最靠前的，直接给它下一个名次编号即可，不用比较其他还没处理的分数

3. 正确性理由：名次本来就是按分数从高到低唯一确定的，每次取出最大值分配当前名次后，剩下的分数排名关系不变，所以逐步取最大值分配名次和整体排序结果完全一致

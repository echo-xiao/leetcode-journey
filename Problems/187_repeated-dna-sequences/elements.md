# 187. 重复的DNA序列 · 要素

1. 定长还是变长：定长，窗口固定是10个字符，即 s[right-10:right] 这段。

2. 进窗口更新：right 每轮加1往右挪一格，取出新的长度10子串放进哈希表 dic，第一次出现记1，已存在就次数+1。

3. 出窗口时机：因为窗口定长，right 前进就等于 left 自动跟着前进一格，不需要显式的 left 收缩，也没有移出元素要维护的数据（每次直接按下标切出新子串）。

4. 记结果时机：当某个子串的计数从1变成2那一刻（即 dic[res]==1 又遇到它）把它加入 ans，保证每个重复子串只记一次。

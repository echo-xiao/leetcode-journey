# 2602. 最多可以摧毁的敌人城堡数目 · 要素

1. 指针类型：用同向而行的快慢指针：i 一直往前扫，idx 留在上一个非零城堡（1 或 -1）的位置上。

2. slow 含义：这里的 slow 就是 idx，指向上一次遇到的非零城堡下标，不是写入位置，而是用来算 i - idx - 1 这段空地长度的左端点。

3. 停止条件：没有相遇一说，fast 指针 i 从 0 走到 forts.length-1 遍历完就停，然后返回 max_cnt。

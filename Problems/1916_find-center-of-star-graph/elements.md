# 1916. 找出星型图的中心节点 · 要素

1. 图的表示：无向图，用哈希表存邻接表（每条边 u-v 双向加），或者干脆只看 edges 数组的前两行不建图。

2. 遍历状态：不用 visited 也不用 onPath，只需统计每个点出现次数/邻居数，看谁是 n-1。

3. 拓扑排序：本题不涉及，星型图无向且只问中心点，没有依赖顺序要排。

4. 最短路松弛：本题不涉及，不求距离，中心到任何点都是一步。

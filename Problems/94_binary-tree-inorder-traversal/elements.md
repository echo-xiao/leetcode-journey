# 94. 二叉树的中序遍历 · 要素

1. 函数定义：traverse(node, res) 表示遍历以node为根的子树，把遍历到的节点值按中序顺序依次加到res列表里，本身不返回值，靠res收集结果

2. base case：当node为空时直接return，不做任何操作，这是递归终止的地方

3. 单层主体：对当前node：先递归处理左子树，再把node.val加入res，再递归处理右子树，左右子树内部已经各自处理好自己的中序结果

4. 代码位置：处理当前节点值（加入res）的代码写在递归左子树之后、递归右子树之前，也就是中序位置

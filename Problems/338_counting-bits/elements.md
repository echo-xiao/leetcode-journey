# 338. 比特位计数 · 要素

1. 状态：状态是当前数字i（0到n之间的每一个整数）

2. 选择：每个数字i的选择是利用i//2（去掉最低位）这个更小的子问题结果，再看i的最低位是0还是1

3. dp 定义：dp[i] 表示数字 i 的二进制表示中 1 的个数

4. base case：dp[0]=0（0的二进制没有1），dp[1]=1（1的二进制只有一个1）

5. 遍历顺序：从小到大遍历i（从0或1一直到n），保证算dp[i]时dp[i//2]已经算好

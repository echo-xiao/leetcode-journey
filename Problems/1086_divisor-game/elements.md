# 1086. 除数博弈 · 要素

1. 状态：状态就是当前轮到自己面对的那个数字 i（从 n 一路减小到 1）。

2. 选择：在数 i 上可以挑任意满足 i%x==0 且 1<=x<i 的 x，把数变成 i-x 交给对手。

3. dp 定义：dp[i] 表示先手面对数字 i 时是否必胜（True 赢、False 输）。

4. base case：dp[1]=False，因为面对 1 找不到合法的 x，先手直接输。

5. 遍历顺序：i 从 2 递增到 n，因为 dp[i] 只依赖比 i 小的 dp[i-x]，小的先算好。

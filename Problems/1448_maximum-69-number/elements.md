# 1448. 6 和 9 组成的最大数字 · 要素

1. 贪心策略：把数字转成字符串从左往右扫，遇到的第一个 '6' 改成 '9'，后面原样保留，只改这一次。

2. 局部最优判据：每一步只看当前这一位是不是 '6'：是 '9' 就跳过继续找，是第一次遇到的 '6' 就立刻换成 '9' 并停下，不用管后面还有几个 6。

3. 正确性理由：位数不变，越靠左的位权重越大（高位加 3 比后面所有低位改动加起来还多），所以把最高位上的那个 6 变成 9 收益最大；而只能改一次，改别的 6 得到的数一定更小。

# 816. 设计哈希集合 · 要素

1. 要不要 dummy：删除操作里用了一个prev指针从链表头开始遍历，相当于dummy的作用，方便处理删除头节点的情况，不需要额外建虚拟头节点，因为bucket本身链表头就可以直接判断

2. 指针关系：本题不涉及快慢双指针，只用了单指针（或prev/cur一对相邻指针）顺序遍历链表查找key，不存在fast/slow速度差关系

3. 停止条件：遍历到当前节点cur为null时停止（即到链表末尾还没找到key），找到key等于目标值时提前返回

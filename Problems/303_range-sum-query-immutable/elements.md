# 303. 区域和检索 - 数组不可变 · 要素

1. 前缀和还是差分：这题是原数组不变、要反复查询区间和，所以用前缀和，不用差分

2. preSum[0] 定义：curr[0]定义为0，表示前0个元素的和，这样查询[0,right]时不用单独判断left=0，直接curr[right+1]-curr[0]就行

3. 差分下标位置：本题不涉及，因为题目不需要对区间做批量增减操作，不需要差分数组

# 2715. K 件物品的最大和 · 要素

1. 贪心策略：每次选物品都优先挑值最大的：先拿完所有1，再拿0，最后不得不拿时才拿-1。

2. 局部最优判据：当前这一步只要看剩下还能不能拿1（k是否≤numOnes），拿不完1再看能不能用0补足（k-numOnes是否≤numZeros），不需要考虑后面怎么拿。

3. 正确性理由：因为总和是各元素独立相加，拿1永远比拿0多1、拿0永远比拿-1多1，所以在数量固定为k的前提下，按值从大到小取满，任何一次用小值替换大值都会让总和变小，所以每步选最大值就是全局最优。

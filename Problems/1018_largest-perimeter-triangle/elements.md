# 1018. 三角形的最大周长 · 要素

1. 指针类型：本题不涉及真正的双指针，只有一个下标 i 顺序扫描降序排好的 nums，同时看 nums[i]、nums[i+1]、nums[i+2] 这三个相邻元素

2. slow 含义：本题不涉及 slow 指针，因为不需要原地写回或去重，只是读取判断 nums[i] < nums[i+1]+nums[i+2]

3. 停止条件：i 走到 len(nums)-3 为止（保证 i+2 不越界），一旦某个 i 满足三角形条件就立刻返回这三边之和，全程没找到就返回 0

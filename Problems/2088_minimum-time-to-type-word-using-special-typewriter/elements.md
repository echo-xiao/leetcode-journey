# 2088. 使用特殊打字机键入单词的最少时间 · 要素

1. 贪心策略：每打一个字符，都从当前指针位置按顺时针 |pos1-pos2| 和逆时针 26-|pos1-pos2| 两个方向里取更小的那个转过去，再加 1 秒敲键。

2. 局部最优判据：只看当前字符 curr 到目标字符 w 的两种绕法距离，直接取 min，不用管后面还有哪些字母。

3. 正确性理由：打完一个字符后指针必然停在这个字符上，后续代价只取决于这个固定的起点，前后步骤互不牵连，所以每步各自取最短转法加起来就是全局最短时间。

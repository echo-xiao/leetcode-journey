# 1128. 删除字符串中的所有相邻重复项 · 要素

1. 栈还是队列：用栈，因为只需要拿当前字符跟最近一个保留下来的字符（栈顶）比，后进先出正好。

2. 存什么：存字符本身，就是已经保留下来、还没被消掉的那些字符，不需要下标或计数。

3. 进出时机：遍历到某个字符时，若栈非空且栈顶等于它，就弹出栈顶且这个字符也不入栈（两个一起消掉）；否则把它压进栈。

4. 剩余元素含义：栈里剩的就是所有相邻重复都消完后的结果字符，从栈底到栈顶拼起来即答案。

# 1574. 数组中两元素的最大乘积 · 要素

1. 贪心策略：直接按数值大小取数组里最大和第二大的两个数，返回 (最大-1)*(次大-1)。

2. 局部最优判据：每扫到一个 num，只看它是否比当前记录的最大值大（大就顶替最大、原最大降为次大），否则看是否比次大值大（大就替换次大），不用管后面还有什么数。

3. 正确性理由：因为题目约束元素都是正整数（≥1），(x-1) 随 x 单调不减，所以两个乘数各自取到最大时乘积必然最大，任何换成更小的数都只会让结果不增，不存在“选小的反而更优”的情况。

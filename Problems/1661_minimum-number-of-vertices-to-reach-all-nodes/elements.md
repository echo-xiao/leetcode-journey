# 1661. 可以到达所有点的最少点数目 · 要素

1. 图的表示：有向图，而且根本不用建邻接表或矩阵，直接扫 edges 数组里的每条 (u,v) 就够了。

2. 遍历状态：只需要一个长度 n 的布尔数组 indegree 标记某点是否被别人指过，不用 visited 也不用 onPath，因为没有真正的遍历过程。

3. 拓扑排序：用到了 Kahn 入度法的第一步思想——收集入度为 0 的点，但不需要真的做完整拓扑排序（不用队列、不用逐层删边）。

4. 最短路松弛：本题不涉及，题目只问哪些点必须当起点，跟距离长短没关系。

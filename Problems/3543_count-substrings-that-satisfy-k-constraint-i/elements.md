# 3543. 统计满足 K 约束的子字符串数量 I · 要素

1. 定长还是变长：变长窗口，窗口大小随左右指针动态变化，不是固定长度

2. 进窗口更新：right每次向右移动一位纳入新字符s[r]，把seen[s[r]]计数加1

3. 出窗口时机：当seen里'0'和'1'的计数都大于k时（即窗口已经不满足最多有一种字符超过k次的约束），就把seen[s[l]]减1并让left右移，直到不再同时超过k

4. 记结果时机：每次right移动、窗口收缩完成后，立刻把(r-l+1)累加到cnt里，因为此时以r为右端点、l到r之间的每个起点构成的子串都满足条件

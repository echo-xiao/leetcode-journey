# 20. 有效的括号 · 要素

1. 栈还是队列：用栈，因为括号是最近未闭合的先匹配，典型后进先出。

2. 存什么：存开括号字符本身（或直接存它对应的那个闭括号），只存字符不存下标。

3. 进出时机：遇到 '(' '[' '{' 就压栈；遇到 ')' ']' '}' 时看栈顶能不能配上，能配就弹出，栈空或配不上直接返回 False。

4. 剩余元素含义：结束后栈里剩的都是没被闭合的开括号，非空说明无效，只有空栈才返回 True。

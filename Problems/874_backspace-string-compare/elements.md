# 874. 比较含退格的字符串 · 要素

1. 栈还是队列：用栈（后进先出），因为退格要删掉的正好是最近加入的字符

2. 存什么：栈里存的是遍历过程中保留下来的普通字符本身（不是下标）

3. 进出时机：遇到非'#'字符就压入栈，遇到'#'且栈非空就弹出栈顶（模拟退格）

4. 剩余元素含义：循环结束后栈里剩下的就是退格处理完之后的最终字符串内容，两个字符串各自构建完的栈内容相等则返回true

# 424. 替换后的最长重复字符 · 要素

1. 定长还是变长：变长窗口，窗口大小不固定，靠left和right动态伸缩来找最大长度

2. 进窗口更新：right每次向右移一位就把新字符计入频率表dic，并更新maxCount为dic中出现次数最多的字符频率

3. 出窗口时机：当窗口长度(right-left)减去maxCount大于k时（说明需要替换的字符数超过k个），就把left指向的字符频率减一并把left右移，直到条件不再满足

4. 记结果时机：每次right右移并完成窗口收缩判断后，用当前窗口长度(right-left)更新maxLen

# 267. 回文排列 II · 复盘

做这个题目可以先做 266、palindrome permutation，因为是easy，其实就是一个找回文字符串的问题， 但是 II 会需要你不仅仅是判断、还有拼接、还有通过回溯来找。是一个排列问题，顺序是活的，要找出所有可能的满足要求的排列。这个里面如果只是按照传统的排列模板会超时，==**核心是针对一半的字母进行排列。虽然大概知道逻辑，但是实际做起来，发现还是不太了解。**==排列里面==**同层去重的逻辑其实还是有点不太懂，主要是not used[i-1]，为什么是 not used[i-1] 而不是 used[i-1] 呢？**==

# 2436. 使数组中所有元素都等于零 · 要素

1. 指针类型：本题其实不用双指针，若硬套只能是排序后同向扫一遍（快指针遍历、慢指针记上一个不同的正数），本质是数不同正数的个数。

2. slow 含义：本题不涉及真正的写入位置，唯一类似 slow 的是 last 变量，它记录上一次统计过的正数值，用来判断当前值是否是新的一档。

3. 停止条件：扫完整个数组（或堆弹空）就停，不存在相遇终止；然后返回统计出的不同正数个数 cnt。

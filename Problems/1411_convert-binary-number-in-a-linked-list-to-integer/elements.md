# 1411. 二进制链表转整数 · 要素

1. 要不要 dummy：不要，本题只是读一遍链表算数值，不新建也不改动节点，head 为空的情况题目也保证不会出现，直接从 head 走即可。

2. 指针关系：本题不涉及双指针，只有一个 curr 指针从头往后走，没有快慢之分。

3. 停止条件：单指针遍历，条件就是 while curr != null，走到空就结束返回 total_value。

# 438. 找到字符串中所有字母异位词 · 要素

1. 定长还是变长：定长窗口，窗口大小固定为p的长度len(p)

2. 进窗口更新：right每次循环都右移一格扩大窗口，把s[right]加入sCount并把它的频率+1

3. 出窗口时机：当窗口大小大于等于len(p)时就要移动left缩小窗口，把s[left]的频率-1，如果减到0就从sCount里删掉这个字符

4. 记结果时机：在窗口大小达到len(p)、还没缩窗之前，先比较pCount和sCount是否相等，相等就把left加入结果列表

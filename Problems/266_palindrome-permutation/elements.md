# 266. 回文排列 · 要素

1. 贪心策略：不是排序取最值型贪心，而是遍历统计每个字符出现次数，边统计边累加奇数次字符的个数oddCnt

2. 局部最优判据：每统计完一个字符就立刻检查oddCnt是否大于1，一旦大于1就可以马上判定不能构成回文并返回False，不用等遍历完

3. 正确性理由：回文串的结构决定了最多只能有一个字符出现奇数次（放在正中间），其余字符必须成对分布在两侧，所以只要发现第二个奇数次字符出现，就必然无法配对成回文，提前终止是安全的，不会漏判也不会误判

# 203. 移除链表元素 · 复盘

这个感觉也不简单啊，做第二遍的时候会错。两个问题：1. ==**不能够在curr节点上删除curr节点，只能够删除curr之后的节点。**==也就是我最开始写错的 `curr.val == val` 的时候，以及正确的 `curr.next.val == val` 写法。2. 当如果有重复的val需要删除的时候，curr=curr.next这个步骤什么时候应该前进，应该在删除节点（if语句）之后执行，还是应该在不删除节点（else语句）之后执行？==**if else 就是一条分叉路口，一定要选择一条**==，但是如果只写一个if，那么无论如何，if条件满足了，并且删除了之后，curr 都会往前走一步。

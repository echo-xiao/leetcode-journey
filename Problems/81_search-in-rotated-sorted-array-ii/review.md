# 81. 搜索旋转排序数组 II · 复盘

binary search 主要处理一段有顺序的数组，但是旋转数组里其实并不完全全部是有顺序的，但是它符合二段性，一个没有重复的旋转数组符合完美的二段性，但是有重复的就不一定了，然后这个题目里面写 if `nums[mid] == nums[right]`：right -= 1 本质其实就是消除干扰，恢复二段星，删掉重复的元素，知道 nums[mid] 和 nums[right] 不相等为止。

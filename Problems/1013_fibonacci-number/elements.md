# 1013. 斐波那契数 · 要素

1. 状态：状态就一个变量 n，表示当前要算第几个斐波那契数。

2. 选择：本题没有真正的「选择」，转移是唯一固定的：第 n 项只能由 n-1 项和 n-2 项相加得到。

3. dp 定义：dp[n] 表示第 n 个斐波那契数的值，记忆化版里 -1 代表还没算过。

4. base case：dp[0]=0，dp[1]=1，递归里 n==0 返回 0、n==1 返回 1 直接结束。

5. 遍历顺序：从小到大算：递推写法 i 从 2 递增到 n；记忆化递归是从 n 往下拆，回溯时天然保证 n-1、n-2 已算好。

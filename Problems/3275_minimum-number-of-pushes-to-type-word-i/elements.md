# 3275. 输入单词需要的最少按键次数 I · 要素

1. 贪心策略：按字符先后顺序，每8个字符分一组，第几组就要按几次键（第1组按1次，第2组按2次，以此类推），不用排序也不用挑值，直接顺序分组。

2. 局部最优判据：每一步只看当前处理到第几组：组号（cost）就是这组每个字符要按的次数，直接把这组字符数乘以组号累加到总数里就是局部最优。

3. 正确性理由：因为按键次数只取决于字符在第几组（每8个字符按键次数加1），各组之间互不影响，所以逐组按组号计算再相加，自然就等于整体最少按键总数，不存在因小失大的情况。

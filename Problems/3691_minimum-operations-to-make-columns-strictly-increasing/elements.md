# 3691. 使每一列严格递增的最少操作次数 · 要素

1. 贪心策略：对每一列从上到下逐个元素处理，只要当前元素不比上一个大，就把它抬升到刚好比上一个大1（上一个值+1），不多加也不少加。

2. 局部最优判据：局部最优判据就是nums[i-1]>=nums[i]时，把nums[i]改成nums[i-1]+1，这是让当前元素满足递增要求且加的次数最少的做法。

3. 正确性理由：每列各元素只依赖前一个元素的最终值，把当前元素抬到刚好比前一个大1不会影响前面已确定的结果，还给后面元素留出了最小的基准值，所以逐个贪心调整的总操作数就是全局最少。

# 1083. 小于 K 的两数之和 · 要素

1. 指针类型：用左右指针相向而行：排序后 left 从头、right 从尾，和太大就 right 左移，和够小就记录答案并 left 右移。

2. slow 含义：本题不涉及，这里不做原地写入或去重，没有 slow 指针，两个指针都是用来试探两数之和的边界。

3. 停止条件：当 left 和 right 相遇时停（while left < right），因为同一个元素不能用两次；循环结束返回 max_num，没找到任何小于 k 的组合就返回初始值 -1。

# 2154. 转换字符串的最少操作次数 · 要素

1. 贪心策略：从左往右扫，碰到第一个没被覆盖的 'X' 就以它为起点用一次操作覆盖 s[i..i+2]，即 i 跳 3 步、计数加 1；是 'O' 就 i 加 1。

2. 局部最优判据：只要当前位置是 'X'，就必须立刻用一次操作，并且这次操作的起点就定在它身上（让覆盖窗口尽量往右延伸），不用看后面还有几个 X。

3. 正确性理由：最左边这个 'X' 必须被某次操作覆盖，而任何能覆盖它的长度为 3 的窗口，覆盖范围都不会超过以它为左端点的那个窗口，所以选它当起点不会更差；每次都这样选，操作次数不会多于任意其他方案，故全局最少。

# 1421. 统计位数为偶数的数字 · 要素

1. 数学结构：就是十进制位数的定义：一个数的位数等于它转成字符串的长度（也等于 log10(n)+1），位数为偶数就计一个，本质是数位统计而不是什么深奥公式。

2. 枚举范围：只需从头到尾扫一遍 nums，每个数各自判断一次位数，没有嵌套枚举；若不转字符串，就是对每个数反复除以 10 最多 5 次（题目数字不超过 10^5）。

3. 数值边界：nums[i] 最大 10^5，位数最多 6 位，全在 int 范围内，计数器最大等于数组长度 500，不会溢出也不用取模。

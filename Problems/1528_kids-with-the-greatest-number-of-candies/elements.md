# 1528. 拥有最多糖果的孩子 · 要素

1. 数学结构：本质是比较运算：只要某孩子当前糖果数加上额外糖果不小于数组最大值，就说明他能拥有最多糖果，用的是最大值的定义性质而非公式推导

2. 枚举范围：本题不涉及枚举优化，只需遍历一遍candies数组求最大值，再遍历一遍做比较判断，没有可以缩小的范围

3. 数值边界：糖果数量和extraCandies都是很小的正整数（题目范围通常在1到100以内），不会溢出，也不需要取模

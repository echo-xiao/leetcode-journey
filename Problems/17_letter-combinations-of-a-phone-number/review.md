# 17. 电话号码的字母组合 · 复盘

就是一个==**组合问题**==，输入的数字顺序是固定的，==**不涉及元素的顺序交换**==，排列问题要考虑元素之间的相对位置。难点在于层数不确定，层数是digits的长度。==**深度就是digits的长度**==，每进入更深一层，就在处理下一个数字。==**宽度就是当前数字映射的字母数量**==，每层里面的for loop循环就是在尝试当前数字能代表的所有可能字符。这样就很清晰了。#组合

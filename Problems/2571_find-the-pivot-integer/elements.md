# 2571. 找出中枢整数 · 要素

1. 前缀和还是差分：用前缀和：1..n 这个数组本身不变，只是反复查 1~x 的和与 x~n 的和，所以先把 arr[i+1]=arr[i]+i 的前缀和算出来查即可。

2. preSum[0] 定义：arr[0]=0，表示前 0 个数的和；有了它，判断中枢时 j=1 也能直接写 arr[n]-arr[j-1]，不用单独处理左边为空的情况。

3. 差分下标位置：本题不涉及，只做查询不做区间加减，没有 diff 数组。

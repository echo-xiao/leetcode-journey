# 783. 二叉搜索树中的搜索 · 要素

1. 函数定义：searchBST(root, val)表示在以root为根的子树中查找值为val的节点，返回值是找到的目标节点，找不到就返回None

2. base case：root为空说明子树中没有目标值，直接返回None

3. 单层主体：比较root.val和val：相等就直接返回root，val小于root.val就去左子树找，否则去右子树找（只会往一边递归，不用管另一边）

4. 代码位置：前序位置，先处理当前节点（判断相等或决定往哪边走），再递归进入子树

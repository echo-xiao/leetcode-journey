# 1062. 将数组分成和相等的三个部分 · 要素

1. 前缀和还是差分：用前缀和思路：数组不变，只是边扫边累加判断和有没有到 total/3，没有任何区间修改，所以不是差分。

2. preSum[0] 定义：这里连前缀和数组都不用建，只用一个 curr 从 0 开始累加、到达 targetSum 就归零；curr 初始为 0 就相当于 preSum[0]=0，省掉了第一段左边界的特判。

3. 差分下标位置：本题不涉及，因为没有对区间做加减操作，只是顺序扫描累加求和。

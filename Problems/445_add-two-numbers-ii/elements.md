# 445. 两数相加 II · 要素

1. 要不要 dummy：要，因为要构建一条全新的结果链表，用dummy节点可以省去处理头节点的特殊判断

2. 指针关系：本题不是快慢指针找位置，而是两个链表各自反转后同步用curr1、curr2两个指针逐位遍历相加，两者步调一致地各自前进

3. 停止条件：当curr1为空且curr2为空且进位carry为0时才停止遍历，只要有一个不为空或还有进位就要继续

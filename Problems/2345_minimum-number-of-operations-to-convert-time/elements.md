# 2345. 转化时间需要的最少操作数 · 要素

1. 贪心策略：把两个时间都换成分钟算差值 diff，然后按 60、15、5、1 从大到小依次尽量多用大面额，操作数就是各步商之和。

2. 局部最优判据：每一步只看当前剩余差值 diff 能整除出多少个当前单位（如 diff//60），全都用掉，不用考虑后面剩下的零头怎么补。

3. 正确性理由：因为 60、15、5、1 这四个面额里每个大的都是小的整数倍（60=4×15，15=3×5，5=5×1），少用一个大单位就必须多用好几个小单位换，只会让次数变多，所以每步取最大面额一定不吃亏，加起来就是全局最少。

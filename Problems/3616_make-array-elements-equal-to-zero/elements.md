# 3616. 使数组元素等于零 · 要素

1. 前缀和还是差分：这里用的是前缀和：数组本身不变，只需要对每个位置i算出它左边元素和prefix[i]、右边元素和suffix[i]，用来判断能不能靠这两部分的和抵消到0。

2. preSum[0] 定义：本题没有单独设prefix[0]=0这种哨兵值，而是直接令prefix[0]=0（左边没元素），suffix[n-1]=0（右边没元素），本质上就是靠它自然省去了边界特判。

3. 差分下标位置：本题不涉及，因为这题用的是前缀和/后缀和判断而不是区间批量增减的差分数组，没有diff[i]、diff[j+1]这种操作。

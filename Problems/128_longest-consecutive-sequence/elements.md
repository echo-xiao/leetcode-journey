# 128. 最长连续序列 · 要素

1. 数学结构：利用「连续序列起点」这个性质：一个数num只有在num-1不存在于集合中时才可能是某段连续序列的开头，靠这个性质避免重复统计同一段序列，从而保证整体线性时间。

2. 枚举范围：只需遍历去重后的集合（用set存num），且只对满足num-1不在集合中的num做向后扩展（查num+1,num+2...），不是起点的数字直接跳过，不用每个数都往后扫。

3. 数值边界：数组元素是int范围内的普通整数，长度和count都是普通计数，不会溢出也不需要取模，本题不涉及边界处理问题。

# 547. 省份数量 · 要素

1. 图的表示：图用输入给的 n×n 邻接矩阵 isConnected 表示，是无向图（isConnected[i][j]==isConnected[j][i]）

2. 遍历状态：只需要一个 visited 数组记录节点是否被访问过，避免重复统计同一个连通分量，不涉及检测环所以不需要 onPath

3. 拓扑排序：本题不涉及，因为省份数量只是求无向图的连通分量个数，跟节点先后顺序或依赖关系无关，不需要拓扑排序

4. 最短路松弛：本题不涉及，因为题目只要统计连通分量数，不需要计算任何路径长度或距离

# 47. 全排列 II · 要素

1. 路径：path，当前已经选进排列里的数字序列

2. 选择列表：nums中所有used[i]为False且没被去重规则跳过的元素，即还没被用过的数字（跳过同层重复且前一个相同数未使用的情况）

3. 结束条件：path的长度等于nums的长度，说明所有数都排完了，把path复制一份加入res

4. 撤销：把path末尾刚加的元素弹出，并把对应位置的used[i]重新设为False，还原成没选这个数的状态

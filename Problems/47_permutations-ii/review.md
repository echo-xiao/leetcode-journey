# 47. 全排列 II · 复盘

其实就是加了一个同层剪枝，专门用来助力数组中有重复元素的情况，这样可以确保结果集里面没有重复的排列。那么如何理解not used[i-1]呢？如果在当前层（横向），现在的数字和前一个数字一样，而且前一个数字刚刚被用完放回去了（used为false），那么就不能够再选择现在的这个数了。如果used[i-1]==True，活命nums[i-1]正在path里，现在是它的下一层。

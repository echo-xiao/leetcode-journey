# 1813. 删除子数组的最大得分 · 要素

1. 定长还是变长：变长窗口，因为要找的是最长的无重复元素连续子数组，窗口大小随元素是否重复动态变化

2. 进窗口更新：right从0遍历到末尾，每次把nums[right]计数加1、窗口和加上nums[right]

3. 出窗口时机：当nums[right]的计数大于1（说明窗口内出现重复）时，就不断移动left，把nums[left]计数减1（减到0就从哈希表删掉）、窗口和减去nums[left]，直到不再重复

4. 记结果时机：每次right移动完成、窗口收缩到不含重复元素后，用当前窗口和更新历史最大得分

# 817. 设计哈希映射 · 要素

1. 要不要 dummy：每个哈希桶的链表在删除节点时可以用一个dummy头节点简化处理头节点被删的边界情况，插入时直接在真实头部插入即可不强制需要dummy

2. 指针关系：本题不涉及快慢指针，这里链表只是用来存同一个桶里的键值对，操作都是单指针遍历查找键是否相等

3. 停止条件：遍历到当前指针为null（即到达链表末尾）就停止，找不到匹配的键就返回-1或不做删除

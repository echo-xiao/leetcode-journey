# 2786. 找到最长的半重复子字符串 · 要素

1. 定长还是变长：变长窗口，因为半重复子串的最大长度不固定，要靠重复对数量的约束动态伸缩

2. 进窗口更新：right每次向右移动一位纳入新字符，若s[right]==s[right-1]则cnt加1（新增一对相邻相同字符）

3. 出窗口时机：当cnt>1（窗口内重复对超过1个）时收缩left，若s[left]==s[left+1]说明左边界处的重复对被移除，cnt减1，然后left右移一位

4. 记结果时机：每次right移动完成、窗口调整合法（cnt≤1）后，用right-left+1更新maxlen

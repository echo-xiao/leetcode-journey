# 1938. 最少操作使数组递增 · 要素

1. 贪心策略：从左往右扫一遍，遇到 nums[i] <= nums[i-1] 就只把它抬到 nums[i-1]+1，一分不多加。

2. 局部最优判据：每到位置 i 只看前一个数：不满足严格递增就补 nums[i-1]+1-nums[i] 次操作，把当前元素设成 nums[i-1]+1，完全不看后面的数。

3. 正确性理由：因为只能加不能减，nums[i] 至少要达到 nums[i-1]+1，这是硬下界；而把它抬得更高只会让后面的数被迫抬得更多，操作数不减反增，所以每步取最小合法值累加就是全局最少。

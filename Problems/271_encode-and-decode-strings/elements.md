# 271. 字符串的编码与解码 · 要素

1. 指针类型：这里用的是解码时的双指针i和j，属于同向而行找分隔符的快慢指针模式，不是左右相向指针

2. slow 含义：本题不涉及slow写入语义，i和j的作用分别是标记当前字符串起点和寻找#分隔符位置，不存在原地覆盖写入的场景

3. 停止条件：j指针在找到分隔符'#'时停止（s[j]=='#'），外层i指针在i小于编码字符串总长度时继续循环，i达到字符串长度即整体遍历结束

# 2121. 寻找图中是否存在路径 · 复盘

首先graph可以理解为有环的多叉树，所以这个里面就需要去知道，我重复探索了这个点，那么就不要探索了，需要有一个地方记录。这个题目，难点主要是3个：1. 知道无向无权图，如何变成一个邻接表。2. 遍历这个里面所有的node，有visit过就返回，没有visit过就打标，如果达到目的地就修正flag，再返回。这个里面的返回/return不是用来返回一个游泳的值的，而是stop的命令，返回空的时候，上级调用者并不会使用这个none里面的值。3. traverse下一个idx的邻居们。

# 2121. 寻找图中是否存在路径 · 要素

1. 图的表示：无向图，用 defaultdict(list) 建邻接表，每条 edges[i]=[u,v] 都要 mapp[u].append(v) 和 mapp[v].append(u) 双向加。

2. 遍历状态：只要一个 visited 集合（或长度 n 的布尔数组）防止无向边来回走导致死循环，不需要 onPath，因为只判连通不判环。

3. 拓扑排序：本题不涉及，无向图判连通性没有依赖顺序可排。

4. 最短路松弛：本题不涉及，只问「有没有路径」不问长度，不需要 distTo 和松弛。

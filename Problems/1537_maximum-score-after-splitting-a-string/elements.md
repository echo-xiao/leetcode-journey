# 1537. 分割字符串的最大得分 · 要素

1. 前缀和还是差分：用前缀和：字符串固定不变，只需反复查询「前i个里有几个0」和「后半段里有几个1」，不涉及区间加减。

2. preSum[0] 定义：arr0[0]=arr1[0]=0，表示空前缀里啥都没有，这样分割点i处直接用arr0[i]和arr1[n]-arr1[i]，不用特判i=0或边界。

3. 差分下标位置：本题不涉及，因为只做区间求和查询，没有区间增减操作。

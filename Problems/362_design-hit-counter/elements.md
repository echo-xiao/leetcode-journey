# 362. 敲击计数器 · 要素

1. 栈还是队列：用队列，因为敲击按时间顺序发生，要先来先处理、按时间先后剔除过期记录

2. 存什么：队列里存的是每次敲击发生时的时间戳

3. 进出时机：每次hit调用时把新时间戳加入队尾；每次getHits调用时，从队头开始把时间差>=300的旧时间戳依次弹出

4. 剩余元素含义：循环结束后队列里剩下的时间戳就是最近300秒内还有效的敲击记录，其数量就是getHits的返回值

# 200. 岛屿数量 · 要素

1. 节点与邻居：每个格子 (r,c) 是节点，上下左右四个方向且值为 '1' 的相邻格子才算邻居，水域 '0' 不连边。

2. 起点与终点：起点是外层双层循环里遇到的每个还没被淹掉的 '1' 格子；没有明确终点，队列空了说明这座岛全被走完，此时岛屿计数加一。

3. visited 时机：入队的同时立刻把 grid[nxtR][nxtC] 改成 '0'（起点也在入队时就改），不能等出队再改，否则同一格会被多个方向重复入队。

4. 是否分层：不用分层，只数连通块个数不关心距离，逐个出队即可。

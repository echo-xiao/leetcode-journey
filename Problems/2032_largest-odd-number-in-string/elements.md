# 2032. 字符串中的最大奇数 · 要素

1. 贪心策略：从右往左扫，找到最靠右的那个奇数位，直接返回 num[0..i] 这一整段前缀。

2. 局部最优判据：每一步只看当前字符 num[i] 是不是奇数（末位是1/3/5/7/9），是就立刻截断返回，不管前面还有什么。

3. 正确性理由：因为答案必须是原串的前缀（不能删中间和开头的字符，否则位数变少或数值变小），前缀长度越长数值越大，而合法前缀的唯一要求是末位为奇数，所以最靠右的奇数位给出的前缀就是最长也是最大的；若一个奇数都没有，则任何前缀都是偶数，返回空串。

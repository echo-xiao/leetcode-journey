# 1720. 文件夹操作日志搜集器 · 要素

1. 栈还是队列：用栈，因为「../」要退回最近一次进入的那层文件夹，正是后进先出。

2. 存什么：存进入过的子文件夹名（其实只关心层数，存什么名字都行，甚至可以只用一个计数器）。

3. 进出时机：遇到普通文件夹名 "xxx/" 就压栈；遇到 "../" 且栈非空就弹一个；遇到 "./" 不动。

4. 剩余元素含义：栈里剩几个元素就是当前离主文件夹的层数，也就是要退回主文件夹所需的最少「../」次数，直接返回栈的大小。

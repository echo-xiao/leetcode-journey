# 3349. 每个字符最多出现两次的最长子字符串 · 要素

1. 定长还是变长：变长窗口，因为要找满足条件的最长子串，长度不固定

2. 进窗口更新：每次r右移一位就把s[r]加入窗口，同时在哈希表seen里把s[r]的计数加1

3. 出窗口时机：当seen[s[r]]大于2（说明当前字符出现超过两次）时就要移动left，把s[l]的计数减1，l右移一位，直到该字符出现次数不超过2

4. 记结果时机：每次内层while循环结束、窗口合法后，用r-l+1和max_length比较取较大值更新

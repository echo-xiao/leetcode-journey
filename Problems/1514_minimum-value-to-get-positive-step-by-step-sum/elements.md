# 1514. 逐步求和得到正数的最小值 · 要素

1. 贪心策略：直接盯前缀和的最小值：把所有前缀和算出来取最小的那个 minSum，答案就是 max(1, 1-minSum)，即刚好把最低点抬到 1。

2. 局部最优判据：每走一步只做一件事：把当前累积和 curr 和已记录的最小值比一比，比它小就更新，不需要知道后面还有什么数。

3. 正确性理由：起始值 x 加到每个前缀和上是整体平移，所有位置同时抬高同样的量，所以只要保证最低的那个前缀和 +x ≥ 1，其他位置必然也 ≥ 1；再小一点最低点就会掉到 0 或负数，所以这个值就是最小的可行解。

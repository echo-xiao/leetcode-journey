# 617. 合并二叉树 · 复盘

semantics就是遍历一遍，然后merge成新的node.val就行，但是不太懂，到底什么时候要如何返回值呢？这个里面，为什么会出现p.left = self.traverse(p.left, q.left)，难道不是直接traverse就行了吗？==**核心区别在于，你的递归函数，是在修改/构建树的结构，还是只读/搜索树的信息**==，如果只是单纯的遍历，只是调用递归就行了，不需要重新赋值，但是如果是改变树的结构，就需要返回一个全新的、构建好的子树的根节点。如果不赋值，原来递归函数辛苦构建好的新子树会被丢弃。

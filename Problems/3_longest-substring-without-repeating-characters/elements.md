# 3. 无重复字符的最长子串 · 要素

1. 定长还是变长：变长窗口，窗口大小随是否有重复字符动态伸缩

2. 进窗口更新：right每次遍历都右移一格，把string[right]加入res列表（窗口内字符集合）

3. 出窗口时机：当res列表长度不等于去重后的集合长度（说明有重复字符）时，就不断删除res最前面的元素并left右移，直到窗口内无重复字符

4. 记结果时机：每次内层while结束、确保窗口内无重复字符后，用当前窗口长度right-left更新maxLen

# 3165. 找出满足差值条件的下标 I · 要素

1. 指针类型：本题用的是同向滑动的双指针（j 向右扫描，min_idx/max_idx 记录 j-indexDifference 范围内的极值下标），不是左右相向指针

2. slow 含义：本题不涉及slow指针写入位置的含义，因为这里不是原地覆盖数组的快慢指针题，min_idx/max_idx 只是记录当前窗口内最小值和最大值的下标

3. 停止条件：停止条件是 j 遍历到数组末尾（j 从 indexDifference 到 n-1 结束）或提前找到满足值差条件的下标对就直接返回

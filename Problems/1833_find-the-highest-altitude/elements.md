# 1833. 找到最高海拔 · 要素

1. 贪心策略：没有真正的贪心选择，就是从0开始一路加gain[i]算出每个点海拔，边加边取最大值。

2. 局部最优判据：每加完一个增量就比一次：curr_altitude 比当前 max_altitude 大就替换，不用回头看后面的元素。

3. 正确性理由：最大值本来就是逐个比较出来的，前缀和又只依赖前面的元素，所以扫一遍就能得到全局最大海拔；本题严格说不算贪心，只是前缀和加打擂台。

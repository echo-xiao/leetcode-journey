# 933. 递增顺序搜索树 · 复盘

两种方式，==**一种是中序遍历**==再加上拼接的过程，但是拼接感觉我也不太懂逻辑。另外一种是==**用stack来进行迭代**==。

---

就是要走中序遍历，然后把所有的节点串起来变成链表，所以这个里面traverse函数要做两件事情，1. 中序遍历traverse每个node 2. 把每个node加入到链表里面去。思维不难，需要注意implementation。

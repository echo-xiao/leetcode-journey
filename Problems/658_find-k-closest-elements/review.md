# 658. 找到 K 个最接近的元素 · 复盘

两种方法可以做，双指针和二分法，这个里面虽然在滑动窗口这个题目题目，但是滑动窗口**labuladong的标准滑动窗口的模板是用来解决“变长窗口的问题”**，这个这个题目其实是变短窗口的问题，所以是对撞双指针。另外一种方法是二分法。#双指针 #对撞指针 #二分查找

---

两种方法可以做，双指针和二分法，这个里面虽然在滑动窗口这个题目题目，但是滑动窗口**labuladong的标准滑动窗口的模板是用来解决“变长窗口的问题”**，这个这个题目其实是变短窗口的问题，所以是对撞双指针。另外一种方法是二分法。#双指针 #对撞指针 #二分查找

---

**这个题目里面滑动窗口（固定大小右移）、双指针（两端开始收缩）、二分法（直接搜索窗口左边边界的最优位置）都可以做；但是我不知道为啥思路从一开始就很混乱；**滑动窗口的例子里面，不是典型的固定窗口的模板，所以这个是不能够用模板的，而是说left从左边开始，然后右边就是一个benchmark，left和right要进行比较，如果right符合条件就要把left踢掉；然后不能用abs这个逻辑，因为 `|a - x| == |b - x|` and `a < b` 这个条件；

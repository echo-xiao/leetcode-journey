# 1127. 最后一块石头的重量 · 要素

1. 栈还是队列：都不是，本题用的是优先队列（最大堆），靠堆序而不是先进先出或后进先出取元素，Python 里存负数把 heapq 的小顶堆当大顶堆用。

2. 存什么：存石头的重量值本身（取负后入堆），不存下标也不存配对信息。

3. 进出时机：入堆：初始化时所有石头重量入堆，以及每次碰撞后差值 y-x 不为 0 时把它入堆；出堆：只要堆里还有 2 块以上石头，就连续弹出最大的 y 和次大的 x。

4. 剩余元素含义：循环结束后堆里最多剩 1 个元素，就是最后一块石头的重量（取负还原）；堆空说明全部两两抵消，返回 0。

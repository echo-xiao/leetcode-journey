# 789. 数据流中的第 K 大元素 · 要素

1. 栈还是队列：本题用的是最小堆而不是普通栈或队列，堆是完全二叉树结构，靠堆顶元素大小来维护第K大，不是简单的先进先出或后进先出

2. 存什么：堆里存的是数据流中较大的K个数值本身，用来保证堆顶就是当前第K大的数

3. 进出时机：新数进来时：如果堆里元素不够K个就直接放进去；如果堆满了K个且新数比堆顶大，就把堆顶弹出、新数放进去，堆顶始终是第K大的值

4. 剩余元素含义：最终堆里剩下的K个元素就是数据流里最大的K个数，堆顶那个就是第K大元素

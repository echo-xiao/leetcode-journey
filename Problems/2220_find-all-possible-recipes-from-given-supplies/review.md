# 2220. 从给定原材料中找到所有可以做出的菜 · 复盘

可以拿这个题目跟 class schedule II 进行比较，==**如果从 bfs （kahn 算法）的角度来看，**==210 的入度是还需要修多少门先修课，2115 的入度是还缺少多少种远离，210的入度的是所有入度为0的课程，但是 2115 的入度是题目给定的初始 supplies。2115 更复杂，因为节点类型不同意，既有基础原料、又有合成后的菜谱、菜谱还能作为原料。同时 210 里面，入度的节点是隐式的，需要遍历整个图去寻找那些没有先修课的节点，2115 里面，supplies 是显示给出来的，需要动态的将 supplies 融入入度逻辑中。==**如果从 dfs （三色标记法）的算法来看，**==就会更加复杂一点。dfs 的回溯，supplies 是终点，只有回溯到它们，这道菜的逻辑链条才是完整的。核心要知道递归具体的stopping case，case 1 如果这个原料在 supplies 里，那么它就是初始材料，递归终止，返回 true，case 2 如果这个原料既不在 recipes 里，也不再 supplies 里，说明根本造不出来，递归终止，返回fasle，case 3 如果遇到了正在查询中的菜谱，说明有循环依赖，返回 false。

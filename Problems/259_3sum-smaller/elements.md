# 259. 较小的三数之和 · 要素

1. 指针类型：用左右指针相向而行：排序后固定 nums[i]，让 j=i+1 从左、k=len-1 从右往中间夹逼找 nums[j]+nums[k] < target-nums[i] 的组合。

2. slow 含义：本题不涉及，这里没有快慢指针也不需要原地写回数组，j 只是固定 i 之后的左边界指针，不是写入位置。

3. 停止条件：内层循环在 j 和 k 相遇时停（while j < k），外层 i 走到 len(nums)-3 为止（保证后面还能取出两个数）。

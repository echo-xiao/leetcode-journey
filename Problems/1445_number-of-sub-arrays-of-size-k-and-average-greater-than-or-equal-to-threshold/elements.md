# 1445. 大小为 K 且平均值大于等于阈值的子数组数目 · 要素

1. 定长还是变长：定长窗口，窗口大小固定为k

2. 进窗口更新：right从0走到k-1时把arr[right]累加进窗口和，构建初始窗口

3. 出窗口时机：从i=k开始每次right前进一格就同时移出最旧元素arr[i-k]，即窗口和=窗口和+arr[i]-arr[i-k]，左右指针同步滑动

4. 记结果时机：每次窗口和确定后（包括初始窗口和之后每次滑动后）立即判断窗口和是否>=threshold*k，满足就把计数器加1

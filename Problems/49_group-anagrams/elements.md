# 49. 字母异位词分组 · 要素

1. 数学结构：数学结构是等价类划分：同一组字母异位词的字符集合完全相同，排序后或计数后的表示必然一致，用这个规范化表示作为哈希键就能把等价的字符串归到一起。

2. 枚举范围：本题不涉及枚举缩放范围，因为不是在数值区间里搜索，而是遍历一遍字符串数组和每个字符串内部字符即可，没有sqrt(n)或质因数分解这类优化点。

3. 数值边界：本题不涉及数值溢出或取模，因为处理的是字符串排序或字符计数，没有大数运算，计数最多也就是字符串长度这种量级，不会溢出。

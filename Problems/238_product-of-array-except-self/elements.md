# 238. 除了自身以外数组的乘积 · 要素

1. 前缀和还是差分：这题用的是前缀积和后缀积（前缀和思路的变体），因为要对每个位置求左右两侧元素的累积乘积，不涉及区间修改，所以不是差分

2. preSum[0] 定义：这题用left[0]（或滚动变量left初始值）表示1，代表位置0左边没有元素乘积为1，这样省去了对i=0的特判

3. 差分下标位置：本题不涉及，因为这题不是区间增减问题，而是求每个位置左右两侧的累积乘积，不需要用到diff[i]和diff[j+1]这种差分标记

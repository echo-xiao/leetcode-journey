# 238. 除了自身以外数组的乘积 · 要素

1. 前缀和还是差分：这题是前缀和的变体，用左侧累乘积和右侧累乘积代替前缀和/后缀和，不需要频繁查询区间，只是各扫一遍算累积结果

2. preSum[0] 定义：本题不用数组存前缀积而是用变量left/right从1开始累乘，left初始为1相当于preSum[0]=1（乘法单位元），天然省掉了第一个位置左边没有元素的特判

3. 差分下标位置：本题不涉及差分，因为这里不需要对区间做增减操作，只是单纯求每个位置左右两侧的累乘积

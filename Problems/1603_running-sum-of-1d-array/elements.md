# 1603. 一维数组的动态和 · 要素

1. 贪心策略：本题不涉及真正的贪心，没有排序也没有取最值，只是从左往右一次遍历把 nums[i] 累加到前一个结果上（arr[i+1] = arr[i] + nums[i]）。

2. 局部最优判据：本题不涉及选择判定，每一步没有「选或不选」，第 i 位的答案被唯一确定为前 i+1 个数之和，没得挑。

3. 正确性理由：本题不涉及贪心正确性证明；正确性来自前缀和的定义递推：arr[i+1]=arr[i]+nums[i]，数学归纳即可保证每个位置都是前缀和。

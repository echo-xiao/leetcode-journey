# 572. 另一棵树的子树 · 复盘

semantics就是判断a某个子树是否会等于b树，那么其实就是判断a是否等于b树，或者a的左子树等于b树，或者右子树等于b树。还是那个问题，**这个里面两个问题：1. 是否需要返回值？ 2. 返回值如何设计？**在当前节点，为了做出决策/计算，我需要的关于子节点的信息，是他的val这种简单属性，还是需要它深入整个子树去计算才能够得到的一个总结性的结论（比如总和、深度、是否相同）？这里就是判断是否相同，所以需要返回值，返回值是左子树和右子树都相同才行。

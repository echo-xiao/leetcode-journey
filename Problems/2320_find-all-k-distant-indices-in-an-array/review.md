# 2320. 找出数组中的所有 K 近邻下标 · 复盘

一种是暴力解法，两个for loop来解，但是看到这种类型的题目，就要考虑是否有优化的方案。然而这个题目的目标又不完全是“连续子数组/子串”，但是很相似，不是要找一个满足条件的子数组，而是要生成全新的索引数组，每个key都会产生一个[j-k, j+k]的影响窗口，最终答案是这些窗口的并集。所以首先是需要先遍历一遍找到key，然后根据每个key去找对应符合条件的窗口。

# 3381. 或值至少 K 的最短子数组 I · 复盘

一开始看，感觉是不固定窗口，最短的满足条件的子序列，需要满足的条件是：符合条件的窗口里面所有的元素位运算≥k，但是这个里面位运算的结果只增不减，A｜B的结果一定≥A，当对更多的数进行位运算的时候，结果只会保持不变或者变的更大，所以这个题目不适合直接用滑动窗口，因为无法逆运算，在现有的窗口中移除left元素的影响，除非要重新计算。所以这个题目更多的是优化过的**暴力解法**。

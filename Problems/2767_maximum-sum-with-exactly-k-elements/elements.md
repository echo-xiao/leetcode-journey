# 2767. K 个元素的最大和 · 要素

1. 数学结构：从数组最大值max开始，取max, max+1, ..., max+k-1这k个连续整数，用等差数列求和公式(首项+末项)*项数/2求和最大

2. 枚举范围：本题不涉及枚举，只需一次遍历或排序找出数组最大值即可，不存在缩小范围的问题

3. 数值边界：结果可能较大（k和max都可能较大导致乘积很大），Python不用担心溢出，但Java/C++等语言需用long类型防止整数溢出，题目不要求取模

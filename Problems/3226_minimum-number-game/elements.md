# 3226. 最小数字游戏 · 要素

1. 贪心策略：把数组从小到大排序，然后每两个相邻的最小值一组，组内把顺序调换（先放第二小的，再放第一小的）

2. 局部最优判据：每次从堆里弹出当前最小的两个数num1、num2，直接判定这两个数在当前所有剩余数里最小，不用看后面剩下的数

3. 正确性理由：因为每对数只要保证第二小的排在第一小的前面就能让Alice和Bob轮流拿数时num2小于等于num1，这个关系只取决于这两个数自身大小，和其他对之间互不影响，所以逐对处理不会影响最终整体排列是否满足题目要求

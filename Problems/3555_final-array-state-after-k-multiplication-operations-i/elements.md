# 3555. K 次乘运算后的最终数组 I · 要素

1. 栈还是队列：本题用的是最小堆（优先队列）而非普通栈或队列，因为需要每次快速取出当前最小值元素

2. 存什么：堆里存的是（元素值，原始下标）的配对信息，方便乘法运算后能放回原位置

3. 进出时机：初始化时把所有元素连同下标一起入堆；每次循环从堆顶弹出最小元素，乘以乘数后再重新压入堆，共重复K次

4. 剩余元素含义：循环结束后堆中剩余的所有元素就是最终结果，把它们按各自保存的原始下标写回结果数组即可

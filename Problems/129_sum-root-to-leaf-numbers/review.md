# 129. 求根节点到叶节点数字之和 · 复盘

能理解整个过程，但是无法自己清晰的写出来，semantics不够清楚，每层的输入、输出、每个node要干什么、终止条件、递归函数如何理解。这个题目是看到答案感觉很简单，但是要思考的话，自己就写不出来。那么首先这个semantics其实就是这个树里面所有的dfs路径之和，所以递归公式是左边+右边的和，所有子树的计算，都需要上一层给到的sum，然后结合这一层的node.val来计算新的sum，这一层需要更新sum，并且返回累加之后的值，终止条件就是左右节点为空就stop了。

# 611. 有效三角形的个数 · 复盘

**1. **习惯性的固定最短边，但是其实应该固定场边，因为公式需要固定某一个遍为benchmark，不然的话不知道到底要移动left还是移动right；**2. **然后我会习惯性的写cnt+=1，但是其实应该是cnt+=(right-left), 因为条件满足的时候，需要利用排序的单调性批量计数，固定right的时候，left到right-1之间所有的值都满足，应该一次加right-left个；最后right的初始值应该携程n-1而不是i-1，两条短边必须在最长边的左边，right应该从i-1开始；

# 2800. 删除子串后的字符串最小长度 · 要素

1. 栈还是队列：用栈，因为要跟“最近未匹配的前一个字符”比较并消除，是后进先出的场景

2. 存什么：栈里直接存字符本身（'A'/'B'/'C'/'D'等），不用存下标

3. 进出时机：每读一个新字符就尝试和栈顶配对：如果栈顶和当前字符组成'AB'或'CD'就弹出栈顶（不压入当前字符）；否则把当前字符压入栈

4. 剩余元素含义：循环结束后栈里剩下的字符就是删除所有'AB'/'CD'子串后无法再消除的最终字符串，其长度就是答案

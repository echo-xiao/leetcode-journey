# 2585. 删除每行中的最大值 · 要素

1. 栈还是队列：都不是，本题用的是每行一个最大堆（优先队列），按值大小出队而不是按进出顺序。

2. 存什么：每行所有元素取负后存进堆里（负值技巧模拟最大堆），存的是值本身，不需要下标。

3. 进出时机：入：一开始把每行元素一次性全压进对应行的堆；出：每一轮（每一列）每个行堆各弹出一个当前最大值，取这些值里的最大者加到 ttl。

4. 剩余元素含义：循环跑完 n 轮后所有堆都空了，没有剩余元素；因为每行元素个数相同，正好被 n 轮删干净。

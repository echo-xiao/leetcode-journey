# 222. 完全二叉树的节点个数 · 要素

1. 函数定义：countNodes(root) 传进来一棵树的根，返回以它为根的这棵子树里一共有多少个节点。

2. base case：root 为空就返回 0，因为空树没有节点。

3. 单层主体：当前节点自己算 1，加上左子树的个数和右子树的个数；优化版则先沿左链、右链量出左右深度，深度相等就说明左子树是满的，直接用 2^深度-1 顶掉一边，只递归另一边。

4. 代码位置：后序位置——必须先拿到左右子树的节点数才能相加返回（优化版的量深度属于进入递归前的前序判断）。

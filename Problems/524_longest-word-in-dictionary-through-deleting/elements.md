# 524. 通过删除字母匹配到字典里最长单词 · 要素

1. 指针类型：这里是同向快慢指针，i指针走s、j指针走word，两个指针都只往前走不回头，用来判断word是否是s的子序列

2. slow 含义：本题不涉及slow指向待写入位置这个含义，因为这里不是原地覆盖数组，而是j指向word中下一个待匹配的字符位置

3. 停止条件：当i走到s末尾（i==len(s)）或者j已经走完整个word（j==len(word)，说明匹配成功）时停止，取先满足的那个

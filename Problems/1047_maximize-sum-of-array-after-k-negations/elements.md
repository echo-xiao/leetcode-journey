# 1047. K 次取反后最大化的数组和 · 要素

1. 贪心策略：每一轮把数组升序排一遍，取反当前最小的那个数（负数优先，负数取完就在最小正数或0上反复翻），重复k次后求和。

2. 局部最优判据：每一步只看当前数组里谁最小，把它取反收益最大：最小的是负数就变正，增量2|x|最大；全是非负数了，翻最小的那个损失最少（有0就损失为0）。

3. 正确性理由：总和等于各元素绝对值之和减去仍为负的那部分的两倍，先翻绝对值大的负数能最快消掉负项；负数翻完后k还有剩，必须继续翻，翻绝对值最小的数带来的减损最少，所以每步的最优选择都不会挡住后面的更好选择，累起来就是全局最大和。

# 2650. 最小和分割 · 要素

1. 贪心策略：把 num 的各位数字按从小到大排序，然后从最小的开始交替塞进 num1 和 num2，让小数字占据高位。

2. 局部最优判据：每次取当前剩下的最小数字，放到当前位数较少的那个数（即下一个更高位置权重更小的那边），不用管后面还剩什么。

3. 正确性理由：两数总和等于各位数字乘以它所在的位权（1、10、100…）之和，两个数位数尽量平均时位权集合固定，剩下就是把小数字配大位权、大数字配小位权，按排序交替分配正好实现这个最优配对，所以结果最小。

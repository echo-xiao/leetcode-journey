# 3517. 新增道路查询后的最短距离 I · 要素

1. 图的表示：用邻接表adj表示有向图，初始时节点i指向i+1，每次查询新增一条u到v的有向边

2. 遍历状态：只需要visited布尔数组标记节点是否访问过来去重，防止BFS重复入队，不需要检测环（本题BFS天然不会走回头路）

3. 拓扑排序：本题不涉及，因为不需要按依赖顺序处理全部节点，只需要每次查询后从0出发做一次BFS求到n-1的距离

4. 最短路松弛：不用dist数组，而是在BFS队列里直接携带(节点,深度)二元组，起点深度为0，每扩展一层邻居深度加1，第一次弹出n-1时的深度就是最短距离

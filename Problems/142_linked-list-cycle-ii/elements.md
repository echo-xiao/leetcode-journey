# 142. 环形链表 II · 要素

1. 要不要 dummy：不用 dummy，这题不改链表结构也不建新链表，只是返回环的入口节点，头节点本身也可能就是入口。

2. 指针关系：第一阶段 fast 每次两步、slow 每次一步（速度差 2:1）；相遇后把 fast 拨回 head，两者改成同速一步一步走，再次相遇处就是入口。

3. 停止条件：第一阶段循环条件是 fast != null && fast.next != null，一旦不满足说明到尾了、无环返回 null；第二阶段停在 slow == fast。

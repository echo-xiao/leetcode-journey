# 1236. 第 N 个泰波那契数 · 要素

1. 状态：状态就是下标 n，表示当前要求第几个泰波那契数。

2. 选择：没有真正的分支选择，只有一条固定转移：第 n 项必须由 n-1、n-2、n-3 三项相加得到。

3. dp 定义：dp[n]（或 recursion(n)）就是第 n 个泰波那契数的值，数组初值 -1 表示还没算过。

4. base case：dp[0]=0，dp[1]=1，dp[2]=1。

5. 遍历顺序：递归写法是从 n 往下递归、回来时自然由小到大填表；改成迭代就从 i=3 递增到 n 顺序算，保证 i-1、i-2、i-3 已算好。

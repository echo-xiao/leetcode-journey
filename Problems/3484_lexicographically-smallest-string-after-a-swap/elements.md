# 3484. 交换后字典序最小的字符串 · 要素

1. 贪心策略：从左到右扫描相邻字符，一旦发现同奇偶且前面数字比后面大就立刻交换，只交换第一处能变小的位置

2. 局部最优判据：当nums[i-1]和nums[i]奇偶性相同且nums[i-1]>nums[i]时，交换这两个字符能让字符串在这个位置变小，就是局部最优

3. 正确性理由：因为只允许交换一次，字典序由高位决定，所以尽早在最靠前的位置把能变小的一对同奇偶数字交换，一定能得到全局最小的字符串，交换靠后的位置或多次交换都不会更优

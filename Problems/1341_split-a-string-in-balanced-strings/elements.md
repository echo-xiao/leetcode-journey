# 1341. 分割平衡字符串 · 要素

1. 贪心策略：从左往右扫，一旦 cnt（R 加一、L 减一）归零就立刻切一刀，能切就切，不往后多看。

2. 局部最优判据：只看当前前缀的 cnt 是否等于 0，等于 0 就说明这段 R 和 L 数量相等，是个合法的平衡串，立刻计数 res+1 并重新开始累计。

3. 正确性理由：每次都在最早能切的位置切，切出的段最短，剩下的后缀依然是整体平衡的（总 R=总 L），仍能继续切；如果推迟切分，只会把两段并成一段，分割数只会变少不会变多，所以最早切一定得到最多段数。

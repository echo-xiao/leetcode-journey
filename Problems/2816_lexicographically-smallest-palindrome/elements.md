# 2816. 字典序最小回文串 · 要素

1. 贪心策略：对每一对对称位置(i,j)，直接取两个字符中较小的那个，同时赋给这两个位置，不需要排序，只是逐对比较取最小值

2. 局部最优判据：当前这一对字符只要都变成min(s[i],s[j])，这一对在保持回文的前提下就已经是能做到的最小值，不用管其它位置怎么变

3. 正确性理由：因为回文串每对对称位置的字符互不影响、彼此独立，把每一对都变成各自能取到的最小值，拼起来整体字典序自然最小

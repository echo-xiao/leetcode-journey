# 111. 二叉树的最小深度 · 要素

1. 函数定义：minDepth(root) 传入一棵子树的根，返回这棵子树里从这个根到最近叶子的节点数（深度）。

2. base case：root 为空返回 0；当前节点左右都空（叶子）返回 1。

3. 单层主体：拿到左右子树的最小深度后，若某一侧为空就只用另一侧的值加 1，两侧都有才取 min(左,右)+1。

4. 代码位置：后序位置——必须先算完左右子树的深度，才能在回来时合并加 1。

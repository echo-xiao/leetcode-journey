# 93. 复原 IP 地址 · 要素

1. 路径：path列表，记录当前已经切分出来的IP段（字符串形式），最多4段

2. 选择列表：从当前start位置开始，取长度为1、2或3的子串作为下一段（前提是长度合法、不含前导零、数值不超过255）

3. 结束条件：path长度达到4段且start正好等于s的总长度时，说明4段刚好用完整个字符串，拼成IP加入结果；若path到4段但start没到末尾则直接返回不记录

4. 撤销：撤销的是path中最后加入的那个分段字符串（path.pop()），把状态还原到选这个分段之前，方便尝试下一个不同长度的分段

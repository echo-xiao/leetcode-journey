# 104. 二叉树的最大深度 · 要素

1. 函数定义：maxDepth(root) 传入一棵子树的根节点，返回这棵子树的最大深度（层数）。

2. base case：root 为空就返回 0，表示空树深度为 0。

3. 单层主体：当前节点只做一件事：拿到左右子树深度后取较大值再加 1（加上自己这一层）。

4. 代码位置：写在后序位置，必须先递归算完左右子树深度才能算当前节点的深度；若换成辅助函数带 depth 往下传的写法，则是在前序位置更新全局 ans。

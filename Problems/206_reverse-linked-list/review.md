# 206. 反转链表 · 复盘

反转这个里面就是需要三个指针，分别是left、right、tmp，这个问题反转问题可以分解成一个A→BC变成A←BC的指针这个问题，然后BC再继续迭代，A的指针进行转向这个动作很简单，所以难点其实在于如何更新指针，这个流程就涉及到“备份-操作-更新-移动”，备份就是right的下一个指针，操作就是right指针的转向，更新是left指针的移动，移动时right指针移到备份节点。

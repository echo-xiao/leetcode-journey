# 323. 无向图中连通分量的数目 · 要素

1. 图的表示：这是无向图，用邻接表存边（或者直接用并查集不显式建图），n个节点，edges数组给出的每条边(u,v)双向连接u和v

2. 遍历状态：DFS/BFS方案只需要一个visited数组标记节点是否访问过来去重，不需要onPath检环（若用并查集则用parent数组代替visited）

3. 拓扑排序：本题不涉及拓扑排序，因为是无向图求连通分量，不存在方向和依赖关系，不需要排序节点顺序

4. 最短路松弛：本题不涉及最短路松弛，题目只求连通分量的个数，不关心节点间的路径长度

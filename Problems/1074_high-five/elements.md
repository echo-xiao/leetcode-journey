# 1074. 前五科的均分 · 要素

1. 贪心策略：每个学生只保留分数最高的5个成绩：用大小为5的最小堆，堆满后遇到比堆顶（当前保留的5个里最小的那个）大的分数就替换掉堆顶。

2. 局部最优判据：当前这条成绩来了，只看两点：堆里不足5个就直接放进去；堆已满就只和堆顶最小值比一次，大就换、不大就丢，完全不用看后面还有什么成绩。

3. 正确性理由：要求前五科均分最大，等价于选出最大的5个分数求和；任何被丢掉的分数都比当前保留的5个都小，它永远不可能进入最终的前五名，所以每一步丢弃都不会损失最优解，遍历完堆里就是全局最大的5个分数。

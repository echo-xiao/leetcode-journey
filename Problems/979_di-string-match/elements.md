# 979. 增减字符串匹配 · 要素

1. 贪心策略：遇到'I'就取当前剩余数字里最小的（left），遇到'D'就取当前剩余数字里最大的（right），这样贪心分配一定能满足相邻大小关系

2. 局部最优判据：局部最优判据就是看当前字符：'I'要求下一个数比当前大，所以填剩余最小值最保险；'D'要求下一个数比当前小，所以填剩余最大值最保险

3. 正确性理由：因为剩余可用数字是一个连续区间[left,right]，取端点填入后剩下的仍是连续区间，不会影响后续可选数字的取值范围，所以每步取端点不会破坏后面构造合法序列的可能性，最终能拼出满足所有I/D关系的排列

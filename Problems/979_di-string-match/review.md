# 979. 增减字符串匹配 · 复盘

这个问题其实跟sort array by parity II的问题有点类似，只不过判断条件不是根据index来进行判断了，而是根据对应的字母来进行判断。其次，这个里面需要自己先创建一个空array但是知道具体的整个array应该填写的是什么的。最后，这个是带有一定程度的贪心算法的思想，先填写大的/小的，然后再去减小/增加。这也不是一个双指针的问题，也不是两次遍历/双向扫描的问题，而是有计数器同时也是数组元素的迭代，只不过这里面两个计数器，一个是从小到大迭代，一个是从大到小迭代。

---

简单

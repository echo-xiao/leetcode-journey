# 1764. 最大重复子字符串 · 复盘

这个题目需要重新的去理解，一方面双指针/滑动窗口就可以直接做出来，但是我就不太会的样子。另外这个题目不像简单的dp题目，递归公式比较难理解，核心是二元选择，遍历整个数组，然后**对每个以i为结尾的窗口做二元决策，匹配则+1并且跳跃，不匹配则分支为0。每个节点是函数调用self.recursion(i), 代表以索引i结尾的序列前缀的最大匹配数。**二元决策就会有if else的组合。这个题目就是在#动态规划 #双指针

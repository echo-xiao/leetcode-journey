# 167. 两数之和 II - 输入有序数组 · 要素

1. 指针类型：左右指针相向而行：left从数组开头出发，right从数组末尾出发，靠两数之和与target的比较来收缩区间

2. slow 含义：本题不涉及，因为这题用的是左右相向指针，不存在快慢指针里“下一个待写入位置”这种含义

3. 停止条件：当left < right（或left <= right）不满足时停止，即左右指针相遇（或交错）时结束循环

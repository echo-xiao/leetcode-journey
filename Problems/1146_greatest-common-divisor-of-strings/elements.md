# 1146. 字符串的最大公因子 · 要素

1. 数学结构：两个字符串有公共因子串的充要条件是str1+str2==str2+str1，此时最大公因子串长度等于len(str1)和len(str2)的最大公约数gcd(l1,l2)

2. 枚举范围：本题不涉及枚举范围缩减，因为求gcd用的是辗转相除法而不是枚举，验证候选串也只需固定次数的重复比较，没有可以缩到sqrt(n)或只看质因数的枚举过程

3. 数值边界：字符串长度和内容都是普通int范围内的值，不涉及大数运算，也不需要取模，唯一要注意的是重复构造字符串时长度不能算错导致越界

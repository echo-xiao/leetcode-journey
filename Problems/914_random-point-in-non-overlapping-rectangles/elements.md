# 914. 非重叠矩形中的随机点 · 要素

1. 前缀和还是差分：这里用的是前缀和，把每个矩形的点数(面积)累加成prefixSum数组，用来按面积比例快速定位随机点落在哪个矩形，不涉及区间修改所以不用差分

2. preSum[0] 定义：这道题prefixSum是从第一个矩形的面积开始累加的（没有额外加一个0开头），二分查找时直接用target在1到ttl之间取值来对应下标，不需要用preSum[0]=0去省特判

3. 差分下标位置：本题不涉及，因为这里没有对区间做批量增减操作，只是单纯累加每个矩形的面积构成前缀和数组

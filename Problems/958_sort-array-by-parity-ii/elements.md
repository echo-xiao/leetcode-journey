# 958. 按奇偶排序数组 II · 要素

1. 指针类型：用两个同向移动的指针even_ptr和odd_ptr，分别只在偶数下标和奇数下标上前进，属于快慢指针（同向而行）的变体

2. slow 含义：本题不涉及slow这种单指针写入语义，even_ptr和odd_ptr各自代表当前需要被填正确奇偶值的偶数位/奇数位置，不是待写入位置的收缩指针

3. 停止条件：当even_ptr超过n-2（越界到最后偶数位之后）或odd_ptr超过n-1（越界到最后奇数位之后）时循环停止，即两指针中任一个先走出数组范围

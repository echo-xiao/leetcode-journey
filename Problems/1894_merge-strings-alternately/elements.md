# 1894. 交替合并字符串 · 要素

1. 指针类型：这里其实是双指针同向同步移动，用同一个索引i分别去访问word1和word2的第i位，不是相向也不是传统快慢指针

2. slow 含义：本题不涉及slow指针，因为不是原地覆盖数组的场景，而是把字符依次append到新结果列表里

3. 停止条件：循环变量i从0走到max(n1,n2)-1，即以两个字符串中较长的那个长度为界，跑完就停，中途通过i<n1和i<n2判断各自是否越界

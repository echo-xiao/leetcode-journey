# 841. 字符的最短距离 · 要素

1. 指针类型：用的是同向双指针（两次单向扫描），从左到右和从右到左各扫一遍，各自维护一个指向最近目标字符位置的指针tmp

2. slow 含义：本题不涉及写入位置的slow指针，tmp表示的是当前遍历方向上最近一次出现目标字符c的下标，用来算距离，不是数组写入位置

3. 停止条件：两次遍历都是指针（i）从字符串一端走到另一端，遇到越界（i超出0到n-1范围）就停止，不存在相遇的情况

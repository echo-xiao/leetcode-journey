# 561. 数组拆分 · 要素

1. 贪心策略：先把数组从小到大排序，然后每两个相邻的数分成一组，取每组里下标为偶数位置(较小)的那个数累加求和

2. 局部最优判据：排序后相邻两个数配对时，取较小的那个纳入求和才能让被舍弃的数尽量小、留下的数尽量大

3. 正确性理由：排序后任意分组方案中，把相邻两数配对能让每组的min值达到最大，因为如果把较小值和更远的数配对会让某个较大数被迫和更小的数配对导致min变小，所以相邻配对+取前者是全局最优

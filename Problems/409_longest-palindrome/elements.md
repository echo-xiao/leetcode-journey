# 409. 最长回文串 · 要素

1. 贪心策略：对每个字符，出现次数是偶数就全部用上，出现奇数就用掉cnt-1个（凑成对），最后如果有剩余奇数字符再额外加1个放中间。

2. 局部最优判据：局部最优判据是看当前字符出现次数的奇偶性：偶数直接全加进res，奇数则减1后加进res并标记hasOdd为True，不需要看其他字符的情况。

3. 正确性理由：回文串由若干对称的字符对加最多一个中心字符组成，每个字符能贡献的成对数量只取决于自身出现次数的奇偶性，互不影响，所以逐字符贪心累加成对数、最后统一补一个中心字符就能得到全局最长长度。

# 2698. 找出数组的串联值 · 要素

1. 指针类型：用左右指针相向而行，left 从 0、right 从 n-1 往中间收，每次配对拼接一次

2. slow 含义：本题不涉及，因为不是原地覆写数组的快慢指针，只是把首尾两数拼接后累加，没有写入位置

3. 停止条件：当 left > right 时停止；left == right 时是最后一次，直接把 nums[left] 加进答案再结束

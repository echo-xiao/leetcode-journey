# 92. 反转链表 II · 要素

1. 要不要 dummy：要用dummy，因为反转区间可能包含头节点（left=1时），加dummy可以统一处理不用特判头节点被替换的情况

2. 指针关系：本题不涉及快慢指针，用的是prev和curr两个指针做头插法反转，curr始终指向待反转区间的第一个未处理节点，prev固定指向反转区间前一个节点

3. 停止条件：不是fast/slow停止模式，而是用计数器循环right-left次（或统计cnt从left到right），循环结束条件是已经处理完right-left+1个节点

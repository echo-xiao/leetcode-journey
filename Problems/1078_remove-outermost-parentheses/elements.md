# 1078. 删除最外层的括号 · 要素

1. 栈还是队列：用栈，后进先出，因为括号配对总是最近的左括号先被匹配掉，也可以退化成一个计数器只记深度

2. 存什么：存左括号本身（其实只用它的个数），栈的高度就是当前嵌套深度，配对信息不用记

3. 进出时机：遇到 '(' 先判断栈是否非空（非空才写进结果）再压入；遇到 ')' 先判断栈长度是否大于 1（大于 1 才写进结果）再弹出

4. 剩余元素含义：因为输入是合法的有效括号串，遍历结束时栈一定为空、计数器回到 0，没有剩余元素

# 79. 单词搜索 · 要素

1. 路径：路径是从起点开始已经走过的格子序列，对应已经匹配上word的前idx个字符

2. 选择列表：选择列表是当前格子(r,c)的上下左右四个相邻格子，前提是没越界且没被访问过

3. 结束条件：结束条件是idx等于word长度说明整个单词都匹配上了返回True，或者当前格子越界/字符不匹配/已访问返回False

4. 撤销：撤销的是把board[r][c]从标记的'#'还原成保存的临时变量tmp原始字符，让这个格子在其他路径里能再次被使用

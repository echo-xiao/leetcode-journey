# 3143. 最长相邻不相等子序列 I · 要素

1. 贪心策略：直接顺序扫描groups数组，只要当前元素和前一个保留下来的元素的group值不相等，就把当前words元素纳入结果，不做任何回溯或比较其他方案

2. 局部最优判据：局部最优判据就是比较groups[i]与上一个被选中元素的groups值是否相等，不相等就必须选（选了不会变差，不选反而会丢长度）

3. 正确性理由：因为每个位置能否被选只取决于它和前一个已选元素的group值关系，跳过一个不相等的元素不会让后面能多选任何东西，反而少了一个，所以每次能选就选一定不劣，最终得到最长的合法子序列

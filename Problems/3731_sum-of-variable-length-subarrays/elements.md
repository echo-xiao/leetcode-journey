# 3731. 变长子数组求和 · 要素

1. 前缀和还是差分：这题是频繁查询区间和，用前缀和：curr[i]表示nums前i个元素的和，用来快速算区间[start, end]的和

2. preSum[0] 定义：curr[0]定义为0，表示空前缀的和，这样计算curr[end+1]-curr[start]时start=0也不用特判

3. 差分下标位置：本题不涉及，因为这题用的是前缀和而不是差分，没有对区间做批量增减的操作

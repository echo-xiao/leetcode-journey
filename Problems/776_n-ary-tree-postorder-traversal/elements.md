# 776. N 叉树的后序遍历 · 要素

1. 函数定义：traverse(node, res)：node是当前N叉树节点，res是用来收集结果的列表，函数没有返回值，靠往res里追加值来记录后序遍历结果

2. base case：如果node为空直接返回，什么都不做（不添加任何值到res）

3. 单层主体：先对node的每个子节点依次递归调用traverse，等所有子节点都处理完了，再把node自己的值加进res

4. 代码位置：属于后序位置，因为要等所有子节点（for循环遍历children递归完）都处理完之后，才把当前节点的值加入结果

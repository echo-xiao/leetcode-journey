# 908. 链表的中间结点 · 要素

1. 要不要 dummy：不需要dummy，本题不修改链表结构也不插入删除结点，直接用原链表头指针操作即可

2. 指针关系：fast每次走两步、slow每次走一步，保持fast的速度是slow的两倍，这样fast到头时slow正好在中间

3. 停止条件：循环条件是fast不为空且fast.next不为空，一旦fast为空或fast.next为空就停止，此时slow即为中间结点（偶数长度时取第二个中间结点）

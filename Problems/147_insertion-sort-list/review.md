# 147. 对链表进行插入排序 · 复盘

分层逻辑，==**一层是守门员逻辑**==，守门员的这个逻辑是一定需要的，一方面是提高效率，==**另外一方面是处理断链，因为node断开之后，我要先接起来**==，接起来我要知道这个lastSorted；你需要知道curr指针是否需要移动，如果>lastSorted（排序好的最后一个），就不需要移动，lastSorted往前走就好；如果是比他小，就到==**另外一层逻辑，**==就需要另外一个prev来进行判断，这个里面的判断，我的理解是==**prev.val≤curr.val<prev.next.val，但是我会卡在把数学逻辑翻译成代码逻辑上面**==，但是恰是只要看右边就好，左边是没有停下来，就说明目前脚下的数字肯定更小，这个条件是天然满足的，只需要去判断下一个人是不是更小。==**核心就是只要前面还太小，就不要停。**==

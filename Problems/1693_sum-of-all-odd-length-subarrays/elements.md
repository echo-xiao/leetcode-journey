# 1693. 所有奇数长度子数组的和 · 要素

1. 前缀和还是差分：用前缀和：arr 不变，只是要反复取出每个奇数长度子数组的区间和。

2. preSum[0] 定义：preSum[0] = 0，preSum[i+1] 表示前 i+1 个元素之和，这样任意区间和写成 preSum[j+1]-preSum[i]，起点 i=0 也不用特判。

3. 差分下标位置：本题不涉及，题目只查询区间和、从不对区间做加减，用不到差分数组。

# 261. 以图判树 · 要素

1. 图的表示：无向图，用大小为 n 的邻接表 adj，每条边 (u,v) 双向都加一次：adj[u] 加 v、adj[v] 加 u。

2. 遍历状态：只要一个大小为 n 的 visited 数组（或集合）去重即可，因为先用 edges.size()==n-1 排除了环，不需要 onPath。

3. 拓扑排序：本题不涉及，无向图判树只需连通性判断，没有依赖顺序可排。

4. 最短路松弛：本题不涉及，只问是不是树，不求任何距离。

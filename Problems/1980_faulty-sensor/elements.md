# 1980. 有缺陷的传感器 · 要素

1. 指针类型：同向而行：一个下标 i 从头同步扫两个数组，找第一个 sensor1[i] != sensor2[i] 的位置；之后再用两条同向游标做错位比较（sensor1 的 i+1 起 对 sensor2 的 i 起，反之亦然）。

2. slow 含义：本题不涉及原地写入，所以没有「下一个待写入位置」的 slow；这里的 i 只表示当前比较到的下标，也就是第一个出现分歧的候选点。

3. 停止条件：主循环 i 走到 n-1 就停（最后一位是丢数据后补的随机值，不参与判断）；错位比较时较短的一边走到末尾即止，全程无分歧则返回 -1。

# 1802. 无法吃午餐的学生数量 · 要素

1. 栈还是队列：两个都用：学生用队列（先进先出，不吃就转到队尾），三明治用栈（后进先出，只取栈顶那个）。

2. 存什么：存的都是 0/1 这个口味值本身，学生队列存每个人想吃的类型，三明治栈存每块饼的类型，不需要下标。

3. 进出时机：队首学生和栈顶三明治口味相同就两个一起弹出、尝试次数归零；不同就把队首学生弹出塞到队尾、尝试次数加一；当尝试次数等于当前学生人数（转了一整圈没人要）时结束循环。

4. 剩余元素含义：学生队列里剩下的人就是没人能配上现存三明治的学生，队列长度即答案；剩下的三明治则是永远没人吃的那些。

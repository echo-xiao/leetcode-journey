# 240. 搜索二维矩阵 II · 要素

1. 指针类型：两个指针不是在一维数组上跑，而是行指针 row 和列指针 col 组成的「Z 字走法」：从右上角出发，row 只增、col 只减，属于相向逼近的那一类。

2. slow 含义：本题不涉及，这里没有原地写数据的 slow，两个下标分别是当前所在的行号和列号，只用来读 matrix[row][col]。

3. 停止条件：当 row 越过最后一行（row >= m）或 col 退到 -1（col < 0）时循环结束返回 False，中途遇到 matrix[row][col] == target 直接返回 True。

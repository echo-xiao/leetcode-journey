# 191. 位1的个数 · 要素

1. 数学结构：核心结论是 n & (n-1) 会把 n 二进制里最低位的那个 1 变成 0（低位全变 1 再进位抵消），所以做几次能把 n 清成 0，就说明有几个 1。

2. 枚举范围：不需要遍历全部 32 位：位操作法只循环 1 的个数次（最多 32 次），逐位除 2 法则循环到 n 变 0 为止，也就是最高位 1 的位置。

3. 数值边界：输入是 32 位整数，用 Python 时 n 恒为非负所以 n>0 的循环安全；在 C++/Java 里若 n 为负数要用无符号或逻辑右移 >>>，否则右移补 1 会死循环，本题不涉及取模和溢出。

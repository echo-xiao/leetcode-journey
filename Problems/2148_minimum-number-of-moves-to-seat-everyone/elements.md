# 2148. 使每位学生都有座位的最少移动次数 · 要素

1. 贪心策略：把 seats 和 students 各自按位置从小到大排序，然后第 i 小的座位配第 i 小的学生，累加 |seats[i]-students[i]|。

2. 局部最优判据：每一步不需要比较别的方案，直接让当前剩下最靠左的学生去坐当前剩下最靠左的座位，这一对的距离就是这步的选择。

3. 正确性理由：如果两对匹配交叉了（小学生配大座位、大学生配小座位），把它们换回不交叉的配法，总距离只会变小不会变大（区间重叠部分被抵消），所以反复交换后最优解一定是排序后一一对应的形式。

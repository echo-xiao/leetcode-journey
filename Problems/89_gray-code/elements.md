# 89. 格雷编码 · 要素

1. 路径：路径是res列表，记录当前已经生成的格雷编码序列（从0开始逐步累加）

2. 选择列表：选择列表是对当前数curr的第i位（i从0到n-1）做异或翻转，得到的nxt若未被访问过就是一个可选选择

3. 结束条件：结束条件是res的长度达到cnt=2^n，说明已经生成了完整的格雷编码序列

4. 撤销：撤销的是把本次加入的nxt从res末尾弹出，并把nxt从visited集合中删除，恢复到选择前的状态

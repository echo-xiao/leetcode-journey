# 35. 搜索插入位置 · 要素

1. 区间定义：用两端都闭的区间[l, r]，初始l=0，r=len(nums)-1，表示当前还没确定插入位置的搜索范围。

2. while 条件：用l<=r，因为闭区间里l==r时那个位置还没检查过，必须再判一次。

3. 判定条件：直接比较nums[m]和target是否相等，因为找的是具体数值的位置，不需要额外的check函数。

4. 边界收缩：nums[m]等于target时直接返回m；nums[m]大于target说明插入点在左边，r=m-1；nums[m]小于target说明插入点在右边，l=m+1；循环结束后l就是插入位置。

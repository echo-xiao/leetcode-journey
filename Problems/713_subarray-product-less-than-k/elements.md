# 713. 乘积小于 K 的子数组 · 要素

1. 定长还是变长：变长窗口，右指针每次移动一格扩大窗口，左指针根据乘积是否超标动态收缩

2. 进窗口更新：right每次循环都右移一步扩大窗口，进窗口时把nums[right]乘进res，然后right再自增

3. 出窗口时机：当res大于等于k时移动left缩小窗口，每次把res除以nums[left]再left自增，直到res小于k为止

4. 记结果时机：每轮内层while结束后（窗口乘积已小于k），用ans += right - left 累加以当前right-1为结尾的合法子数组个数

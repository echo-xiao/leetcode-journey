# 3773. 移除最小数对使数组有序 I · 要素

1. 要不要 dummy：本题不涉及，因为这道题用的是数组模拟和暴力遍历找最小相邻对，不是链表结构，不存在创建新链表或头结点的问题

2. 指针关系：本题不涉及，本题没有快慢指针，只有遍历数组找相邻元素和最小值的普通索引变量

3. 停止条件：本题不涉及，循环的停止条件是数组变为非递减顺序（if_non_decreasing为True时退出），不是链表遍历到某个节点为空

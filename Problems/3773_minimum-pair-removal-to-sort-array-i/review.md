# 3773. 移除最小数对使数组有序 I · 复盘

这个是array的题目，而不是linked list的题目。直接模拟“只要数组不是有序的，那就找到最小的和相邻队，合并，并且重复这个过程“，这是一种暴力解法，那么这个数组是否有序，就用到了布尔标记的方式，先判断是否有序，判断是否有序是用一个for loop来做，如果loop完了之后，找相邻元素，然后删掉这两个元素，insert新的元素，完全利用的array的基础性质。

# 2231. 找出数组中的第一个回文字符串 · 要素

1. 指针类型：用左右指针相向而行：在每个字符串上放 left=0、right=len(word)-1，两头往中间夹着比字符。

2. slow 含义：本题不涉及，这里不是快慢指针，也没有原地改写数组，没有「下一个待写入位置」这种东西。

3. 停止条件：内层循环在 left >= right 时停（即两指针相遇或交错，说明整串都对称，是回文）；一旦 word[left] != word[right] 就提前跳出判为非回文；外层遍历到第一个回文就返回它，全部走完没找到则返回空字符串。

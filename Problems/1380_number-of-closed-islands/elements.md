# 1380. 统计封闭岛屿的数目 · 要素

1. 节点与邻居：节点就是每个格子坐标 (i,j)，邻居是上下左右四个相邻且值为 0（陆地）的格子。

2. 起点与终点：起点分两批：先是四条边界上所有值为 0 的格子（用来淹掉通海的陆地），之后再遍历内部剩下的每个 0 作为新起点；没有终点，队列空了就算这块陆地扫完，最终返回内部起点的个数。

3. visited 时机：本题不用额外 visited 数组，把格子值直接改成 1（水）就等于标记已访问，出队/入队时立刻改，避免同一块陆地反复入队。

4. 是否分层：不用分层，只要把连通的陆地全部淹没并计数，不关心步数或距离。

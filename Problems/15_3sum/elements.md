# 15. 三数之和 · 要素

1. 指针类型：用左右指针相向而行：left从固定数i的下一位开始，right从数组末尾开始，向中间夹逼找和为target的两数

2. slow 含义：本题不涉及，因为本题用的是左右指针夹逼两数之和，不是快慢指针同向遍历，没有slow这种写入位置的概念

3. 停止条件：每一轮内层循环的停止条件是left < right不成立即两指针相遇（left>=right）时停止，外层i循环遍历到n-1结束

# 15. 三数之和 · 要素

1. 指针类型：用左右指针相向而行，在固定nums[i]后，left从i+1出发、right从数组末尾出发向中间夹逼查找两数之和

2. slow 含义：本题不涉及，本题是相向双指针求和问题，没有快慢指针写入数组的场景

3. 停止条件：right和left在同一个while循环里移动，停止条件是left小于right不成立即左右指针相遇或交叉时结束该轮内层循环

# 1280. 健身计划评估 · 要素

1. 定长还是变长：定长，窗口固定是连续 k 天的卡路里，长度永远不变。

2. 进窗口更新：right 从 k 一路走到末尾，每步把 calories[right] 加进和 T 里（开头先把前 k 个元素累加成初始 T）。

3. 出窗口时机：每次 right 进一个就必须马上出一个，T 减去 calories[left] 再 left++，保证窗口长度还是 k。

4. 记结果时机：每形成一个完整的 k 长度窗口就判一次：T<lower 则 cnt--，T>upper 则 cnt++，中间不动；包括初始那个窗口也要判，最后返回 cnt。

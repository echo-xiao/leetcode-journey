# 226. 翻转二叉树 · 要素

1. 函数定义：invertTree(root)：传入一棵子树的根节点，把这棵子树整体左右翻转，返回翻转后的根节点（还是原来那个 root）。

2. base case：节点为空就直接返回空，不用交换也不用往下递归。

3. 单层主体：当前节点只干一件事——把它的 left 和 right 两个指针对调，剩下的左右子树交给递归各自翻转。

4. 代码位置：前序和后序都行（前序先交换再递归左右，后序先递归左右再把返回结果对调）；中序要写成递归左、交换、再递归新的左，否则会把同一棵子树翻两次。

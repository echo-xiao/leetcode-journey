# 496. 下一个更大元素 I · 要素

1. 单调方向：栈内保持从栈底到栈顶单调递减，这样栈顶元素才可能是当前元素的下一个更大元素候选

2. 存下标还是值：这道题栈里存的是值（num），因为要直接用它和后面元素比大小，再存到mapp里做映射

3. 弹栈条件：当栈顶元素小于等于当前遍历到的num时，说明栈顶元素找到了更大的元素（就是num），把它弹出

4. 遍历方向：倒着遍历nums2（从后向前），这样能保证每个元素在入栈时，栈里保留的都是它右边还没被淘汰的更大候选

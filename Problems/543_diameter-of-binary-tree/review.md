# 543. 二叉树的直径 · 复盘

这个的semantics可以这么理解，是一个遍历的方式，其实就是要找这个树的最大直径，经过这个node的最大直径就是左数最大深度+右树最大深度，那么遍历每个node，然后更新这个全局的最大直径就可以了。这个里面另外一个公式，计算深度是计算的节点数，计算直径计算的是边的数量，边的数量是节点数-1，但是最后加上node的话，其实就是(leftDepth-1)+(rightDepth-1)+2，这个里面的2是root连接两边的node。

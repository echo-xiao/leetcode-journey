# 225. 用队列实现栈 · 要素

1. 栈还是队列：底层容器只能用队列（先进先出），但对外要表现成栈（后进先出），靠 push 后把队头元素轮转到队尾来倒序。

2. 存什么：队列里就存 push 进来的那些整数值本身，没有下标也没有配对信息。

3. 进出时机：push 时先把 x 入队尾，再把它前面的 n-1 个元素依次出队重新入队，让 x 顶到队头；pop/top 直接取队头，也就是最近入栈的元素。

4. 剩余元素含义：本题不涉及，这是设计数据结构题没有遍历循环；队列里留着的就是当前栈中还没弹出的元素，队头到队尾对应栈顶到栈底。

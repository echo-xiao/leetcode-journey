# 230. 二叉搜索树中第 K 小的元素 · 要素

1. 函数定义：dfs(node) 不返回值，作用是中序走一遍树，靠外部变量 k 倒数、res 存答案，k 减到 0 时 res 就是第 k 小的值。

2. base case：node 为空直接 return，什么都不做（空树没有节点可数）。

3. 单层主体：当前节点是中序序列里的下一个数：先走完左子树，然后 k 减 1，若 k 变成 0 就把 node.val 存进 res 并返回，否则继续走右子树。

4. 代码位置：写在中序位置（左子树递归和右子树递归之间），因为只有中序才让 BST 节点按从小到大出现。

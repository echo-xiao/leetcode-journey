# 1019. 有序数组的平方 · 要素

1. 指针类型：用左右指针相向而行：i 指头、j 指尾，因为原数组两端的绝对值最大，平方最大值只可能出现在两端。

2. slow 含义：本题不涉及快慢指针；对应的写入指针 k 从结果数组末尾开始，指向下一个要填的位置，每填一个就左移一位。

3. 停止条件：外层循环条件是 i <= j，当 i > j（两指针交错）时停止，此时正好填满 n 个位置；注意必须用 <=，否则中间那个元素会漏填。

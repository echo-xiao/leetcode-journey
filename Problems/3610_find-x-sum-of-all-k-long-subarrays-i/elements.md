# 3610. 计算子数组的 x-sum I · 要素

1. 贪心策略：每个子数组内按元素频率从高到低（频率相同按元素值大小）排序，直接取前x个元素求加权和，不用管其它元素排列

2. 局部最优判据：当前候选元素的频率是否排在本窗口所有元素频率的前x名（用最小堆或排序后取前x比较）

3. 正确性理由：因为x-sum的定义就是取频率最高的x个元素加权求和，每个子数组的计算相互独立，只要保证窗口内选出的x个元素是频率最大的x个即为该子数组的最优解，不存在跨窗口的依赖关系

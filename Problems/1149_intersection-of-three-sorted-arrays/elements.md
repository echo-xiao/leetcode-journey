# 1149. 三个有序数组的交集 · 要素

1. 指针类型：三个都是同向前进的指针 p1、p2、p3，各自在自己的数组里从左往右走，不是一个数组内的左右夹逼。

2. slow 含义：本题不涉及：不是原地覆盖写入，三个指针都只是各数组的读取位置，结果单独存进 res 列表。

3. 停止条件：任一指针走到自己数组末尾（p1>=len(arr1) 或 p2>=len(arr2) 或 p3>=len(arr3)）就结束循环，因为后面再不可能凑出三方相同的值。

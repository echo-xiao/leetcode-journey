# 2692. 从数量最多的堆取走礼物 · 要素

1. 栈还是队列：本题用的是最大堆（优先队列），不是普通栈或队列，因为每次都要取当前最大值而不是按顺序进出

2. 存什么：堆里存的是每堆礼物的数量（用负数表示以模拟最大堆）

3. 进出时机：每次循环开始时弹出堆顶最大值，计算其平方根取整后再把结果压回堆中，重复k次

4. 剩余元素含义：循环k次结束后，堆中剩下的所有元素就是最终每堆礼物的数量，把它们求和取负就是答案

# 924. 公平的糖果交换 · 复盘

这其实是一个找精准值的过程。有三种方式，第一种是哈希表，通过代数的计算找到要找的精准值，然后用再进行判断，是最快的方式；第二种是排序后binary search，这个套路就是set的替代品，如果题目限制了内存，可以用binary search来找精准值；最后一种方法是排序+双指针，两个指针分别指向两个array的head，每次需要计算交换总差值来跟0相比判断是否fair，然后在每一步与目标的差值来判断移动方向。

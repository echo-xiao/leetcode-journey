# 2128. 反转单词前缀 · 要素

1. 指针类型：用左右指针相向而行：先扫一遍找到第一次出现字符 ch 的下标 k，然后 left=0、right=k 两头往中间交换，把前缀 s[0..k] 原地反转。

2. slow 含义：本题不涉及快慢指针，不需要 slow 做写入位置——反转是原地两两交换，没有「筛掉元素再写回」的动作。

3. 停止条件：两个指针在 left >= right 时停（相遇或交错即完成前缀反转）；另外找 ch 的那一遍扫描若走到字符串末尾都没找到，就直接返回原串。

# 456. 132 模式 · 要素

1. 单调方向：从后往前遍历时，栈里保持从栈底到栈顶单调递减，这样栈顶始终是当前能作为‘2’的候选最大值

2. 存下标还是值：存的是值（nums[i]本身），因为要用它们更新k（也就是潜在的‘2’这个数），不需要下标信息

3. 弹栈条件：当栈顶元素小于当前遍历到的元素nums[i]时，说明栈顶可以被淘汰当作‘2’，弹出栈顶并更新k为这个被弹出的值

4. 遍历方向：倒着遍历（从数组末尾往前遍历），这样能先确定‘3’和‘2’再往前找‘1’

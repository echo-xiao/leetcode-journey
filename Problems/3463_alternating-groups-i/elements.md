# 3463. 交替组 I · 要素

1. 定长还是变长：定长窗口，窗口大小固定为3（因为交替组的最小判定单位是连续三个元素）

2. 进窗口更新：right每次前进1，把arr[l+2]纳入窗口，判断arr[l]、arr[l+1]、arr[l+2]是否满足相邻不同色

3. 出窗口时机：每次窗口固定为3个元素，l每次前进1就相当于移出最左边的元素，不需要额外维护累计状态

4. 记结果时机：当窗口内arr[l]≠arr[l+1]且arr[l+1]≠arr[l+2]时，说明这三个点构成交替组，cnt加1

# 207. 课程表 · 复盘

这是拓扑排序的经典入门，拓扑排序就是有向无环图的节点进行线性排序，使得所有的关系都满足先序要求。这个题目其实就是在问，有没有环。可以通过flags状态，来进行判断。三种方式来做，一种是递归，用flags来标记是否是祖先（是否有环），是否走过，是否安全；一种是stack，判断是否有环，但是里面的逻辑比较繁琐；最后一种是queue，基于入度的逻辑进行推演，模拟先修课才能够后续上课的过程，**核心就是拓扑排序（Kahn算法）**。其实是没有太搞懂的。#topologicalSort

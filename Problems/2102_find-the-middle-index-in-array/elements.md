# 2102. 找到数组的中间位置 · 要素

1. 前缀和还是差分：用前缀和：数组不动，只是要反复查每个下标左边的和与右边的和，所以先把 arr 前缀和算出来再逐个比较。

2. preSum[0] 定义：arr[0] = 0，表示前 0 个元素的和；这样 j=0 时左侧和直接取 arr[0]=0，不用单独判断最左端。

3. 差分下标位置：本题不涉及，题目只查询区间和、没有区间增减操作，所以用不到差分数组。

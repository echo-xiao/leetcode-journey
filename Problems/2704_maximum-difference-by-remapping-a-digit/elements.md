# 2704. 替换一个数字后的最大差值 · 要素

1. 贪心策略：求最大值时把字符串中第一个不是'9'的数字全部替换成'9'，求最小值时把第一个不是'0'的数字全部替换成'0'，取两者之差最大化差值

2. 局部最优判据：每一步只看当前字符是否等于目标数字（'9'或'0'），一旦找到第一个不等的字符就立刻选它替换，不用比较其他位置

3. 正确性理由：数字越靠前的位权重越大，把最高位能变的数字尽量变大（变9）或变小（变0）对整体数值的提升/降低幅度最大，且因为原数字所有出现的相同字符位置都同步替换，所以第一次出现即是全局收益最大的选择

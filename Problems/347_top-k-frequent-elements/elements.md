# 347. 前 K 个高频元素 · 要素

1. 存什么：堆里存的是(频率, 元素)这样的元组，方便按频率排序又能取出对应元素

2. 序：用小顶堆，按频率字段比较，堆顶始终是当前堆中频率最小的元素

3. 容量：堆容量固定为k，超过k个就把频率最小的弹出去

4. 进出时机：遍历哈希表统计出的每个(元素,频率)时push进堆，一旦堆大小超过k就立刻pop堆顶（最小频率的），遍历结束后堆里剩下的k个就是答案

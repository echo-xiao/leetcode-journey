# 39. 组合总和 · 要素

1. 路径：path列表，记录当前已经选入组合的数字（可重复使用同一个数）

2. 选择列表：candidates数组里下标从start到末尾的所有数，即当前数还能继续选自己或之后的数

3. 结束条件：target减到0时把path加入结果集并返回，target小于0时直接返回不再往下搜

4. 撤销：递归调用结束后把path末尾刚加进去的那个候选数弹出，把target恢复成加这个数之前的状态

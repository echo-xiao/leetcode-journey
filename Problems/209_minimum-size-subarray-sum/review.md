# 209. 长度最小的子数组 · 复盘

用滑动窗口来做最简单，minLen = min(minLen, right-left) 这个条件要放在left里面去，因为符合条件就需要更新。但是如果是复杂的逻辑，比如里面有复数，滑动窗口就不合适了，需要用到二分搜索，但是逻辑有点绕，需要前缀和+滑动窗口，要遍历数组里面每个点，然后找这个点到后面哪一个点，判断中间的距离/差值要超过target value。#滑动窗口 #二分搜索

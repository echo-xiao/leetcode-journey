# 2337. 移除指定数字得到的最大结果 · 复盘

==**核心难点在同时处理三个条件，首先是跳过**==，需要跳过_，找到下一个有意义的字符，各自跳，但是节奏不同，这里要想while什么时候停止；==**第二个是顺序要一致**==，L和R的相对顺序要一样，这里要洗那个，跳完之后两个指针是不是都到头了，还是只有一个到头了；==**最后一个是方向的约束**==，L只能够左移，R只能够右边移动，这个要想字符一样之后还要检查位置对不对；

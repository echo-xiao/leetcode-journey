# 694. 不同岛屿的数量 · 要素

1. 图的表示：网格grid本身就是隐式的无向图，每个值为1的格子是节点，上下左右相邻的1之间有边，不需要额外建邻接表或矩阵。

2. 遍历状态：用grid[r][c]置0代替visited数组标记已访问，避免重复遍历，同时记录访问路径的方向序列（DFS递归版）或相对坐标（DFS迭代版）来表示岛屿形状，不需要onPath检环因为网格DFS不会成环。

3. 拓扑排序：本题不涉及拓扑排序，因为岛屿遍历不涉及依赖顺序或环检测，只是单纯的连通块遍历。

4. 最短路松弛：本题不涉及最短路径计算，题目关心的是岛屿的形状去重计数，不是求路径长度或距离。

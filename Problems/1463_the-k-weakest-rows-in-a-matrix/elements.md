# 1463. 矩阵中战斗力最弱的 K 行 · 要素

1. 栈还是队列：本题其实都不是——用的是优先队列（大顶堆），靠战斗力大小排序出队，不是按先进先出或后进先出。

2. 存什么：堆里存的是配对信息 [-战斗力, -行号]，取负是为了用小顶堆模拟大顶堆，保证战斗力大（或行号大）的先被踢掉。

3. 进出时机：遍历每一行时先算出该行1的个数就入堆；一旦堆里超过 k 个，立刻把堆顶那个战斗力最强（或战斗力相同但行号最大）的弹出。

4. 剩余元素含义：遍历结束后堆里剩下的正好是战斗力最弱的 k 行；因为是大顶堆，逐个弹出得到的是从弱到强的倒序，取出 -idx 后整体反转才是答案。

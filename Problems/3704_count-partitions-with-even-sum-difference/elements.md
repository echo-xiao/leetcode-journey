# 3704. 统计元素和差值为偶数的分区方案 · 要素

1. 前缀和还是差分：这题是要频繁查询区间和来算左右两段的和，所以用前缀和，不涉及区间批量增减，不需要差分

2. preSum[0] 定义：preSum[0]定义为0，这样curr[i]就代表nums前i个数的和，分区点i从1到n-1都能直接用curr[i]和curr[n]-curr[i]取到左右和，不用特判空数组的情况

3. 差分下标位置：本题不涉及，因为这道题只是求前缀和判断分区，没有对区间做批量加减的差分操作

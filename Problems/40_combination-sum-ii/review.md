# 40. 组合总和 II · 复盘

跟 39 的差别在于，39、combination sum 是“元素可无限重复使用”切“输入无重复”，但是 40、combination sum II 是“每个位置的元素只能够用一次”切“输入包含重复元素（需要去重）”。所以会有一个横向去重，不能够进长的一样的门，同时也有纵向去重，不准吃回头草。**start 是当前层的起跑线，idx+1是下一层的新边界，确保下一层递归不会再次选中当前的 i（除非题目允许重复选取，那么就选择 idx，不然就是 idx+1）。**

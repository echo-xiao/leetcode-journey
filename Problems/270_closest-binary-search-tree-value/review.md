# 270. 最接近的二叉搜索树值 · 复盘

==**如何insert target value到树里面**==，不太会。

---

返回整棵树跟target最接近的node，就等于返回左子树跟target最接近的node，或者右子树跟target最接近的node，然后这个往左还是往右是需要经过判断的，这个是定向搜索。定向搜索，左右子树分别遍历之后每次的返回值，然后根据这个返回值，返回值到target的举例，和target到节点的距离，进行比较，判断最终返回哪个值。这个里面最关键的其实是要获取返回值，拿返回值进行比较。

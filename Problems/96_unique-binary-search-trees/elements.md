# 96. 不同的二叉搜索树 · 要素

1. 状态：状态是当前节点数i（从0到n），表示用i个不同的节点能组成多少种二叉搜索树

2. 选择：选择的是以哪个节点作为根节点，根节点为j时左边有j-1个节点、右边有i-j个节点

3. dp 定义：dp[i] 表示由i个节点组成的二叉搜索树一共有多少种不同的形态

4. base case：dp[0]=1（空树算一种），dp[1]=1（一个节点只有一种树）

5. 遍历顺序：从小到大遍历i（从2到n），因为算dp[i]要用到dp[j-1]和dp[i-j]这些更小的子问题，必须先算完

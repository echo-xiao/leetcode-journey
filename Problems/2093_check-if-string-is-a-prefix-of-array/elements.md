# 2093. 检查字符串是否为数组前缀 · 要素

1. 指针类型：这题其实是同向推进：一个指针遍历 words 数组，另一个指针记录 s 已经匹配到的位置，不需要左右相向的双指针。

2. slow 含义：slow 就是 s 上已经匹配掉的字符个数（下一个要比对的 s 下标），words 里每拼进一个词就往后推该词的长度。

3. 停止条件：外层指针遍历完 words 就停；只要拼接结果等于 s（即 slow 正好走到 s 末尾）就立刻返回 true，中途出现字符对不上或长度超过 s 也能直接返回 false。

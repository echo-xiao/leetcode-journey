# 904. 叶子相似的树 · 要素

1. 函数定义：traverse(node, res) 表示遍历以node为根的子树，把遇到的叶子节点值依次加进res列表，没有返回值；leafSimilar(root1,root2)返回两棵树叶子序列是否相同的布尔值

2. base case：node为空时直接返回，不做任何操作（不会把空节点当叶子处理）

3. 单层主体：判断当前node是不是叶子（左右孩子都为空），如果是就把node.val加入res，左右子树的遍历已经在递归调用里完成不用管

4. 代码位置：后序位置，因为要先递归完左右子树，最后再判断并处理当前节点是否是叶子

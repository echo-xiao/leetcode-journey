# 395. 至少有 K 个重复字符的最长子串 · 复盘

这个题目有点儿难，模板不管用，因为这个里面是有一些坏的元素的（元素出现的次数<k），而不是找满足好元素的条件，这个不是普通的滑动窗口模板，是要用分治法，切成好几个符合条件的子字符串，然后再判断最长。其次，在找最长的时候，要通过元素来进行切分，且分了之后要用到递归，每次递归之后，都需要重新统计频率、重新找坏的字符串。以及，递归会涉及到终止条件，最后就是返回条件怎么写了。#分治法 #divideAndConquer

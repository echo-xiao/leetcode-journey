# 2634. 最小公共值 · 要素

1. 指针类型：用同向而行的双指针，但不是快慢指针，而是分别指向 nums1 和 nums2 的两根指针 i、j，谁指的值小谁往后走。

2. slow 含义：本题不涉及，这里没有原地写入，两根指针分别属于两个数组，只做比较和推进，不存在「下一个待写入位置」。

3. 停止条件：当 i 走到 nums1 末尾或 j 走到 nums2 末尾（任一越界）就停，循环里一旦 nums1[i]==nums2[j] 立即返回该值，正常退出循环则返回 -1。

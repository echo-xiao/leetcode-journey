# 131. 分割回文串 · 要素

1. 路径：path 里存已经切好的那几段回文子串，比如 s="aab" 走到一半时 path 可能是 ["a","a"]。

2. 选择列表：从当前下标 start 开始，往后枚举结尾 i（start 到 n-1），得到子串 s[start..i]，只有它本身是回文才算一个可选的选择。

3. 结束条件：start 等于 s 的长度，说明整个字符串都切完了，把 path 的拷贝放进 res 然后返回。

4. 撤销：递归回来后把刚加进 path 的那段子串 pop 掉，让 path 回到加它之前的样子，好去试下一个更长的切法。

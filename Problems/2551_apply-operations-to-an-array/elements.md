# 2551. 对数组执行操作 · 要素

1. 指针类型：用快慢指针（同向），fast 和 slow 都从下标 0 出发往右走，把非零数往前搬；左右指针不适用，因为要保持原有相对顺序。

2. slow 含义：slow 指向下一个非零元素该放的位置，也就是 [0, slow) 里已经排好了所有非零数，遇到 nums[fast]!=0 就交换到 slow 处再 slow++。

3. 停止条件：fast 一直往右走到越界为止（fast == nums.length 停），不存在两指针相遇的情况；之后 slow 之后的位置自然全是 0。

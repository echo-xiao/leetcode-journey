# 170. 两数之和 III - 数据结构设计 · 要素

1. 栈还是队列：本题不涉及栈或队列，用的是哈希表（数字→出现次数），元素进出没有先后顺序要求。

2. 存什么：存的是哈希表键值对：键是 add 进来的数字，值是这个数字出现的次数（次数用来判断 value 是两个相同数字相加的情况）。

3. 进出时机：只有「进」没有「出」：每次 add(number) 就把该数字的计数加一，find 只读不删，所以不存在弹出时机。

4. 剩余元素含义：本题不涉及，哈希表里始终保存所有 add 过的数字及其次数，find 遍历结束返回 False 只说明没有配对，不代表有残留结构。

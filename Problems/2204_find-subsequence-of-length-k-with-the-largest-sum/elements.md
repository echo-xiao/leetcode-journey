# 2204. 找到和最大的长度为 K 的子序列 · 要素

1. 贪心策略：按元素值从大到小挑出最大的 k 个数（用最小堆或排序实现），挑完再按原始下标排回去输出。

2. 局部最优判据：每遇到一个数就看它是否比当前已选 k 个里最小的那个（堆顶）大，大就换掉堆顶，不用管后面还有什么数。

3. 正确性理由：子序列和只取决于选了哪 k 个数、跟顺序无关，所以要和最大就必须选最大的 k 个；如果某个被选的数比没选的小，把它换成更大的那个和一定变大，说明不选最大 k 个不可能最优；而任意 k 个数都能按原下标排出一个合法子序列，所以这个选法一定可行且最优。

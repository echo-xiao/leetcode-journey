# 3788. 删除后的最大子数组元素和 · 要素

1. 贪心策略：去重后只保留每个不同数值一份，能选就选所有非负数，不能选（全是负数）就只能挑最大的负数，贪心维度是数值本身是否非负。

2. 局部最优判据：对去重后的每个数单独判断：如果它≥0就一定要加进结果（因为加正数或0只会让和变大或不变），否则就不选。

3. 正确性理由：因为数组元素之间没有位置或数量限制（重复元素去重后互不影响、可任意子集组合），所以每个非负数独立决策是否加入不会影响其他数的选择，局部选“非负就加”累加起来自然是全局最大；而当没有非负数时，只能选一个数，选最大的负数显然最优。

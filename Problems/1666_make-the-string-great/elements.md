# 1666. 整理字符串 · 要素

1. 栈还是队列：用栈，后进先出，因为要消除的总是刚放进去的那个相邻字符。

2. 存什么：直接存字符本身，最后拼起来就是答案，不需要下标。

3. 进出时机：遍历到某字符时，若栈顶和它同字母但大小写不同就弹出栈顶且当前字符也不入栈（一起抵消掉）；否则把当前字符压栈。

4. 剩余元素含义：栈里从底到顶剩下的字符就是再也无法配对消除的部分，按顺序拼接即为整理后的字符串。

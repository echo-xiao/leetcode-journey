# 1519. 非递增顺序的最小子序列 · 要素

1. 贪心策略：把 nums 从大到小排序，每次优先把当前最大的数放进子序列。

2. 局部最优判据：每加一个数就看前缀和 arr[i] 是不是已经严格大于剩下的 ttl-arr[i]，一旦大于就立刻停下返回前 i 个数，不用管后面还有什么。

3. 正确性理由：要让子序列长度最短，同样个数下取最大的几个数总和最大，最容易先超过一半；而先满足长度最短、再要求和最大，降序取前缀正好同时做到这两点，所以第一个超过一半的前缀就是答案。

# 83. 删除排序链表中的重复元素 · 复盘

==**首先**==是维护一个指针维护链表，还是两个指针。先说==**一个指针的方法**==，一些会错的点在于如 while 条件，不能是curr.next.next 不为空，因为这样的话，会有最后一个node没有检查到，==**其次**==需要 while curr 和 curr.next 都不为空的情况，不能够只写一个，因为如果 curr 为空，那么 curr.next 就不行了。==**以及**==要判断head是否为空的情况。==**如果是两个指针的情况**==，那么if条件下就需要移动nxt指针。

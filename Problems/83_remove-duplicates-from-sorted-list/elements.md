# 83. 删除排序链表中的重复元素 · 要素

1. 要不要 dummy：不需要dummy，因为不删头节点、也不是新建链表，头节点本身不会变，直接用head当起点即可

2. 指针关系：这里不是快慢指针追赶关系，而是当前节点cur和cur.next做值比较，相等就把cur.next跳过去指向cur.next.next，不相等cur才前移

3. 停止条件：当cur为空或cur.next为空时停止遍历，因为此时没有下一个节点可比较了
